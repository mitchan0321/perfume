defun man (name) {
    is ::= new Stream;
    $is puts ["LANG=ja_JP.UTF-8 MANWIDTH=80 MAN_KEEP_FORMATTING=t man " . $name];
    $is close;
    pipe in: $is
        {r ::= $stdin gets; exec sh -c $r}
        {$stdin each do: {| r | println [curs-col $r]}}
    ;
};
