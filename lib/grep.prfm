# $Id:$
# grep

defun grep (pat file) {
    set f `File(mode: i $file);
    try {
	set n 1;
	$f each do: {| r |
	    if {$r =~ $pat} then: {
		print $file ":" $n ": " $r;
	    };
	    $n ++;
	};
    }
    fin: {
	$f close;
    };
    println;

    true;
};
