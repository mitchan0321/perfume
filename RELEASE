2024/08/15	PRFM_20240815		1.11.1
2024/07/14	PRFM_20240714		1.11.0
2024/06/26	PRFM_20240626		1.10.0
2024/02/14	PRFM_20240214		1.9.1
2023/12/23	PRFM_20231223_K35	1.9.0
2023/06/05	PRFM_20230605		1.8.1
2023/05/01	PRFM_20230501		1.8.0
2023/03/21	PRFM_20230321		1.7.2
2023/02/15	PRFM_20230215_A34	1.7.1
2022/12/23	PRFM_20221223_K34	1.7.0
2022/10/25	PRFM_20221025		1.6.3
2022/10/06	PRFM_20221006		1.6.2
2022/09/10	PRFM_20220910		1.6.1
2022/07/27	PRFM_20220727_PLASMA	1.6.0
2022/06/22	PRFM_20220622		1.5.29
2022/06/15	PRFM_20220615		1.5.28
2022/06/07	PRFM_20220607		1.5.27
2022/06/02	PRFM_20220602		1.5.26
2022/05/23	PRFM_20220523		1.5.25
2022/05/05	PRFM_20220505		1.5.24
2022/04/19	PRFM_20220419		1.5.23
2022/04/15	PRFM_20220415		1.5.22
2022/04/01	PRFM_20220401		1.5.21
2022/03/14	PRFM_20220314		1.5.20
2022/03/10	PRFM_20220310		1.5.19
2022/02/28	PRFM_20220228		1.5.18
2022/02/18	PRFM_20220218		1.5.17
2022/02/16	PRFM_20220216		1.5.16
2022/02/15	PRFM_20220215_A33	1.5.15
2022/02/14	PRFM_20220214		1.5.14
2022/02/13	PRFM_20220213		1.5.13
2022/02/03	PRFM_20220203		1.5.12
2021/12/23	PRFM_20211223_K33	1.5.11
2021/11/11	PRFM_20211111		1.5.10
2021/09/20	PRFM_20210920_N33	1.5.9
2021/08/08	PRFM_20210808		1.5.8
2021/07/08	PRFM_20210708		1.5.7
2021/06/19	PRFM_20210619		1.5.6
2021/06/10	PRFM_20210610		1.5.5
2021/05/26	PRFM_20210526		1.5.4
2021/04/09	PRFM_20210409		1.5.3
2021/03/12	PRFM_20210312		1.5.2
2021/01/04	PRFM_20210104		1.5.1
2020/12/23	PRFM_20201223_K32	1.5.0
2020/12/09	PRFM_20201209		1.5.0_RC_1
2020/09/10	PRFM_20200910		1.4.1
2020/04/15	PRFM_20200415		1.4.0
2020/03/18	PRFM_20200318		1.4.0_PRE_2
2020/03/13	PRFM_20200313		1.4.0_PRE_1
2020/02/21	PRFM_20200221		1.3.0
2020/02/19	PRFM_20200219		1.2.9_PRE_2
2019/12/02	PRFM_20191202		1.2.9_PRE_1
2019/10/16	PRFM_20191016		1.2.8
2019/08/23	PRFM_20190823		1.2.7
2019/06/25	PRFM_20190625		1.2.6
2019/06/17	PRFM_20190617		1.2.5
2019/05/27	PRFM_20190527		1.2.4
2019/04/01	PRFM_20190401		1.2.3
2019/02/26	PRFM_20190226		1.2.2
2019/02/15	PRFM_20190215_A30	1.2.1
2019/01/07	PRFM_20190107		1.2.1_PRE_1
2018/12/23	PRFM_20181223_K30	1.2.0
2018/11/21	PRFM_20181121		1.2.0_PRE_1
2018/05/17	PRFM_20180517		1.1.1
2018/05/15	PRFM_20180515		1.1.0
2017/06/14	PRFM_20170614		1.0.1
2017/03/21	PRFM_20170321_OMAPERO15	1.0.0
2017/03/03	PRFM_20170303		1.0.0_RC_3
2017/03/01	PRFM_20170301		1.0.0_RC_2
2017/02/22	PRFM_20170222		1.0.0_RC_1
2017/01/18	PRFM_20170118		1.0.0_PRE_2
2017/01/16	PRFM_20170116		1.0.0_PRE_1
2017/01/12	PRFM_20170112		0.9.3
2016/03/12	PRFM_20160312		0.9.2
2016/03/02	PRFM_20160302		0.9.1
2016/02/15	PRFM_20160215_A27	0.9.0
2016/02/02	PRFM_20160202		0.8.1_p4
2016/01/14	PRFM_20160114		0.8.1_p3
2016/01/07	PRFM_20160107		0.8.1_p2
2015/12/31	PRFM_20151231		0.8.1_p1
2015/12/30	PRFM_20151230		0.8.1
2015/12/23	PRFM_20151223_K27	0.8.0
2015/12/21	PRFM_20151221		0.8.0_RC_4
2015/12/18	PRFM_20151218		0.8.0_RC_3
2015/12/17	PRFM_20151217		0.8.0_RC_2
2015/12/13	PRFM_20151213		0.8.0_RC_1
2015/12/09	PRFM_20151209		0.8.0_PRE_6
2015/11/24	PRFM_20151124		0.8.0_PRE_5
2015/11/23	PRFM_20151123		0.8.0_PRE_4
2015/11/15	PRFM_20151115		0.8.0_PRE_3
2015/10/23	PRFM_20151023		0.8.0_PRE_2
2015/10/22	PRFM_20151022		0.8.0_PRE_1
2015/10/01	PRFM_20151001		0.7.2
2015/09/25	PRFM_20150925		0.7.1
2015/09/20	PRFM_20150920_N27	0.7.0
2015/08/09	PRFM_20150809		0.6.0
2015/02/15	PRFM_20150215_A26	0.5.0
2014/12/09	PRFM_20141223_K26	0.4.0
2012/07/16	TOY_20120716		0.3.0
2012/06/10	TOY_20120610		0.2.0
2011/08/28	TOY_20110828		0.1.3
2009/11/01	TOY_20091101		0.1.2
2009/09/23	TOY_20090923		0.1.1
2009/09/05	TOY_20090905		0.1.0
2009/07/27	TOY_20090727		0.0.28
2009/07/06	TOY_20090706		0.0.27
2009/05/17	TOY_20090517		0.0.26
2009/05/10	TOY_20090510		0.0.25
2009/03/28	TOY_20090328		0.0.24
2009/03/08	TOY_20090308		0.0.23
2009/03/01	TOY_20090301		0.0.22
2009/02/22	TOY_20090222		0.0.21
2009/02/21	TOY_20090221		0.0.20
2009/02/19	TOY_20090219		0.0.19
2009/02/15	TOY_20090215		0.0.18
2008/12/10	TOY_20081210		0.0.17
2008/11/01	TOY_20081101		0.0.16
2008/10/04	TOY_20081004		0.0.15
2008/09/04	TOY_20080904		0.0.14
2008/08/31	TOY_20080830		0.0.13
2008/08/30	TOY_20080830		0.0.12
2008/08/24	TOY_20080824		0.0.11
2008/08/23	TOY_20080823		0.0.10
2008/08/22	TOY_20080822		0.0.9
2008/08/19	TOY_20080819		0.0.8
2008/08/16	TOY_20080816		0.0.7
2008/08/13	TOY_20080813		0.0.6
2008/08/08	TOY_20080808		0.0.5
2008/08/05	TOY_20080805		0.0.4
2008/07/27	TOY_20080727		0.0.3
2008/07/26	TOY_20080726		0.0.2
2008/07/25	TOY_20080725		0.0.1

--
2008/07/25	
	initial CVS repository imported.

2008/07/26
	eval.c:
		rewrite toy_eval, to non recurcive function call.
	commands.c:
		set?/sets?/unset/unsets added.
	methods.c:
		String::=/String::!=/Object::string added.
		Integer::-- added.
	add setup.toy script.
	add config.h file.

2008/07/27
	object constructor implementation. 'init' method at object creation.
	Hash class implementation.
	setup.toy:
		add apply-constructor script.

2008/08/05
	eval.c:
		bind_args, change algorithm.
	array.c:
		add array type.
	methods.c:
		add File class, Array class and modify Hash for using any type in key value.
	get macro implementation, for example: $object,$key meaning [$object get $key].

2008/08/08
	commands.c:
		print / println change, use $@out, $stdout File object.
	eval.c:
		add toy_call C function, for using from C native function.
	Variable 'argv' move to global slot, and change name to 'ARGV'.
	methods.c:
		add mth_file_set as File::set! for set file descriptor.
	parser.c:
		parse get macro extention bug fix.
	setup.toy:
		add stdin / stdout / stderr variables setup.

2008/08/13
	output global variable '@stack-trace' if generated exception.

2008/08/16
	commands.c:
		add stack-trace command.
		add trap command for UNIX signal handling.
	eval.c: add siglan handling.
		fix LIST extend bug.
	methods.c:
		add List::join method.
		change List::+ method.
		add String::eval method.
		add String::+ method.
	Closure variable is able to reference in function and method.

2008/08/19
	commands.c:
		add ! / and / not command.
		add load command.
		add sid command.
	eval.c:
		change to stack-trace / load support.
		add to omit stack-trace output flag.
	hash.c:
		change no use to 'strlen'.
	interp.c:
		change get_stack_trace for print script file name.
	parse.c:
		fix to symbol parse.
	types.c:
		add TAG_NAMED_MASK into SYMBOL.

2008/08/22
	commands.c:
		fix sid command for specified FUNC object.
		add sdir command.
	methods.c:
		fix List::append degrade, return last item cons cell.
		add Array::last method.

2008/08/23
	Makefile:
		add install script.
		add config.h.in configuration.
		add VERSION indicate.
	commands.c:
		add pwd, cd command.
		add alias command.
	hash.c:
		add ALIAS type procedure.
	interp.c:
		add VERSION indicate.
		add ENV variable setup.
	parser.c:
		fix '..' symbol parse, but adhockly.
	type.[ch]:
		add ALIAS type.

2008/08/24
	Makefile:
		add onigruma library added.
		mearge Makefile.nogc, Makefile.prof into Makefile.
	type.[ch]:
		add type RQote for Regexp string.
	methods.c:
		add String::match/String::=~ method.
	parser.c:
		add RQuote parse.

2008/08/30
	cell.c:
		add cell_sub for sub-string return.
	methods.c:
		fix String::=~ and add switch 'all:' and 'case:'.
		add String::sub command.

2008/08/31
	methods.c, eval.c:
		change for init method parameter useful.
	eval.c:
		add to_string_call for Object::string.

2008/09/04
	eval.c:
		add switch argument, ":XXX" meaning same as named arg "XXX: 1".
	*:
		add INITMACRO.
		ex)
			set o [new Hash arg: (((a . 1) (b . 2)))];
		     same as:
			set o `Hash((a . 1) (b . 2));
	methods.c:
		change String::=~ option case: => nocase:
		add Hash::init
		add Array::init

2008/10/04
	methods.c:
		when dividing by zero, ErrZeroDivide is generated.

2008/11/01
	eval.c:
		to signal block execute under closure environment.

2008/12/07
	methods.c:
		add List::eval
	cell.c:
		change GC_MALLOC to GC_MALLOC_ATOMIC for allocate cells data.

2009/01/17
	commands.c
		add sleep command.
		fix self command.
	setup.toy
		add method Block::>> for command outpu redirect file method.

2009/01/21
	commands.c
		change defvar and add setvar.

2009/01/24
	Makefile
		setup.toy install directory change to /usr/local/lib/toy.

	commands.c
		add internal function 'println'.
		change 'defvar' to define global once.
		add 'setvar' can set to global when variable already defined.
		add 'defvar?' check global variable existing.
		bug fix 'self' return to legal object.
		bug fix load nested case.
		add file command.
		add exists? command.

	methods.c
		add List::add like List::append but returns self object.
		add String::. like the String::+ but safely.
		add File::ready? for event occurrence check.

	interp.c
		change "setup.toy" load by "load" command.

	setup.toy
		add global variable INTERACTIVE for shell mode indicator.
		$HOME/.toyrc load if file existing.

2009/02/15
	methods.c
		add method List::filter, List::map

2009/02/17
	*
		syntax change: var: X -> in: X

2009/02/19
	Makefile
		install lib directory to $PREFIX/toy/lib.
	eval.c,	setup.toy
		add 'unknown' feature.
	lib/*
		add grep.toy, wc.toy.
	Syntax change
	       all iterator var: keyword change to in:.
	commands.c
		cmd_print bug fix.
		sid return nil if no func match.
		add type? command.

2009/02/21
	commands.c
		'if' command syntax extend, if cond-block is value then eval value.
		add command rand.
	methods.c
		add method List::flat
		add method String::<, >, <=, >=
		add method Array::list
		add method Integer::..
	setup.toy
		load $LIB_PATH/util.toy.
	$LIB_PATH/
		some command separate into this directory.
	toysh.c
		print result length limit to 512 bytes.

2009/02/22
	eval.c
		to_string_call bug fix.

2009/03/01
	methods.c
		add method List::>> (shift), List::<< (unshift),
			List::->> (pop), List::<<- (push)
		add method List::seek
	msort.toy
		new added sample script merge sort program.

2009/03/07
	commands.c, eval.c
		add trace command.

2009/03/08
	commands.c
		trace syntax added for body tracing.
		add exit command.
	interp.toy
		add feature toy script for command interpriter.
		for using: interp

2009/03/28
	Makefile
		gcc flag '-Wall' add.
	commands.c
		set command fix, make hash link if variable into closure.
		add fork command.
	eval.c
		fix search method faild when self method call.
	methods.c
		File::gets bug fix when EOF return "".
		Array::init bug fix.
	SysQueue.toy
		add error handling.

2009/03/30
	commands.c
		bug fix String constructor mangled.
	commands.c, methods.c
		in progress add callcc.

2009/05/10
	*
		Any iterator syntax change.
		    old style) List::each in: i do: {block}
		    new style) List::each do: {| i | block}

2009/05/17
	*
		add lazy feature.

2009/07/06
	commands.c
		change case syntax.
		       old) case var (val {body} ...)
		       new) case var val {body} ...
		add new command cond, like the lisp cond function.
		add lazy command.
	eval.c
		bug fix Object::string.
	methods.c
		add method Object::method? as search object method.
	setup.toy
		buf fix unknown.
	lib/ls.toy
		add option :all for recurcive list.
	lib/util.toy
		add Hash::each method.

2009/07/27
	*
		add memory allocate error detect code.
		fix interpriter stack operation bug.
		change Object::init method spec, arg: (val ...) -> init: (val ...)
		change default interpriter stack size.
	commands.c
		try command fix, if occurd exception in fin: block then return exception.

2009/09/05
	hash.c, eval.c
		add global cache feature. If this feauture turn on, specify HAS_GCACHE in Makefile.
	commands.c
		delete 'echo' command.
		call command syntax added, call object (params ...).
		add cache-info if determin HAS_GCACHE.
	methods.c
		add constructor to File class.
	lib/wc.toy, grep.toy, cat.toy
		change code,  use File constructor.
	lib/util.toy
		add Object::_ method.
		add File::each method.
	tests/*
		fix results file.
	*
		if parameters less than 6, optimizing parameter bind process.
		change stack trace design and optimizing.

2009/09/23
	commands.c
		'new' command recognize variable $LIB_PATH, if not defined class auto loading.
		fix print/println error message.
		add begin command for local environment isolate.
		add exec command for external file execution.
		add read command for read line from $stdin/@in or specified file-object.
	interp.c
		bug fix in toy_add_method.
	lib/*
		fix grep command.
		fix interp.toy by new command auto load feature and command line evaluate
		in local stack.
		add new command flat for display all of the object/class methods.
	lib/util.toy
		add Block::<< method.

2009/11/01
	*
		symbolized keyword parameter (e.g. then:, else:, do: ...)
	eval.c, commands.c
		add goto feature.
	types.[ch]
		add type DICT.
	commands.c
		add dict.
	methods.c
		add object Dict.
		add method String::split.

2009/11/21
	types.[ch]
		add type VECTOR.
		add type COROUTINE.
	lib/*
		add memoiz.toy for auto create memoization function.
		add resolv-host command for IPv4 feature.
	commands.c
		add dict-local, dict-object, dict-func, dict-global, dict-class commands.
		add vector.
		add eq? command.
		add connect, resolv-in-addr command for IPv4 client feature.
		add coro, pause for coroutine support.
		add gc command.
		add loop command.
	methods.c
		add List::inject.
		add method DICT::xxx, VECTOR::xxx.
		delete Object::eq.
		add String::int, String::real, String::number, String::rquote.
		add Integer::real, Real::int.
		add Integer::<<,>>,||,&&,^^,~~ for bit operation.
		File::??? add mode: to 'io' for input/output mode.
		add Coro::next method for coroutine support.
	*
		long long int to int64_t.

2011/08/28
	array.[ch]
		change internal implementation.
		change initial size 8 to 256.
	eval.c
		method select bug fix.
		change lazy call default.
	hash.c
		bug fix that deferent variable reference on union.
	methods.c
		add String::fmt method.
		add native Vector::each method.
	lib/*
		a few changes.

2011/12/09
	cstack.c
		add feature C machine stack management mechanizm.
	commands.c
		add command symbol?, nil?, list?, integer?, real?,
		string?, rquote?, block?/closure?, func?, object?,
		dict? and vector?.
	eval.c
		to_string bug fix when type is dict and vector.
	methods.c
		add method Object::apply
	parser.c
		add macro ':' continuas separator macro.
	util.toy
		add method List:insert!, List:: delete!, List::find,
		List::find* and List::reverse.

2012/03/01
	cstack.c
		C native stack divid used mechanizm from co-routine.
		C native stack barriered by mmap(2).
	eval.c
		Function parameter number set order changed.
	toysh.c
		Interpriter restart when if main C native stack overflowed.
	types.c
		add co-routine handler.
	lib/slist.toy
		add command for show stack.
	lib/Y.toy,fact-Ycombi.toy,feb.toy,start-interp.toy
		add thease commands.

2012/03/06
	eval.c, commands.c, interp.c, types.h
		Lexical scope changed.
	Y.toy, fact-Ycombi.toy
	        Sample for Y-combinator.

2012/06/10
	cstack.[ch]
		Support 32bit/64bit environment.
		(Use __PTRFIFF_TYPE__ macro)

2012/07/05
	lib/util.toy
		List::find, List::find* bug fix.
	parser.h
		Fix symbol parse end condition, add "'" character.
	lib/fib.toy
		Bug fix.

2012/07/10
	commands.c
		Add multiple variable set feature to 'set' command.

2012/07/16
	types.[ch], interp.c, methods.c, commands.c
		Add bit integer implementation.

2014/12/23
	A programming langage 'perfume' rename from 'toy-lang'.
	eval.c
		Delete command result variable '?'.
	*
		Delete class Hash, Array and CALL/CC.
		begin command option name change, nocascade -> rebase.
	lib
		Update lsort.prfm and msort.prfm for use getter block.
		Add asort.prfm for vector quick sort.
	parser.c
		Add '\' macro. (e.g. A B \ C D -> C D [A B])
	methods.c
		Add vector::swap.
		Add Real::sqrt.
	array.c
		Add array_swap.
	doc
		Update docment.
	tag:
		PRFM_20141223_K26
		0.4.0

2015/02/15
	util.prfm
		Add global variable PI, E.
	methods.c
		Add Integer::^
		Add Real::sin
		Add Real::cos
		Add Real::tan
		Add Real::asin
		Add Real::acos
		Add Real::atan
		Add Real::log
		Add Real::log10
		Add Real::exp
		Add Real::exp10
		Add Real::pow
		Fix Object::apply
	cstack.[ch] / types.c / methods.c / eval.c / perfumesh.c
		Co-routine stack overflow handling robustness.
	interp.prfm
		Interpriter using co-routine.
		Last command status saved in interp. For refered by "result :last" command.
	Makefile
		Add -lm link option.
	commands.c
		Add cstack-release command for force release cstack slot.
		Add coro-id command.
		Add :last option to result command.
		Add REM command.
	*
		C stack overflow signal handling change.
	tag:
		PRFM_20150215_A26
		0.5.0

2015/07/30
	methods.c / commands.c / global.[ch]
		Add option :nonewline and :nocontrol at File::gets.
		Add option :nonewline and :nocontrol at read.
	lib/util.prfm
		Add Integer::!.
	eval.c / commands.c
	        Add closure variable update command 'setc' and check command 'setc?'.

2015/07/31
	methods.c
		Integer::+,-,*,/,%,=,!=,>,>=,<,<=
		Real::+,-,*,/,=,!=,>,>=,<,<=
		Calculation of numerical value between different types became possible.

2015/08/03
	methods.c
		Fix String::fmt that %d and %f indicate accept different type.

2015/08/06
	methods.c / global.[ch]
		Fix String::fmt if some arguments are omitted, replace to null string ("").

2015/08/09
	tag:
		PRFM_20150809
		0.6.0

2015/08/12
	type.c
		Fix signed integer into big integer type bug.
	lib/slist.prfm
		Fix display address bug.

2015/09/12
	cstack.c
		Change allocate stack implementation.
	lib/interp.prfm
		Add message at exiting intepriter.
	commands.c
		File object auto GC implementation.
	type.c
		Coro object auto GC implementation.

2015/9/20
	config.h.in
		Co-routine stack slot number change to 256.
	lib/slist.prfm
		Add memo field as the executing script printing.
		Add :wide and :all option. Default narrow and using slot only display.
	cstack.[ch], types.c
		Add memo field for executing script display.
	methods.c
		String::=~ using for regex cache.
		List::+ rename to List::append!.
		String::+ rename to String::append!.
		Vector::+ rename to Vector::append!.
	lib/util.prfm
		Add String methods are +, -, *, /, %, ^, &&, ||, ^^, ~~, sin, cos, tan,
		asin, acos, atan, sqrt, pow, exp10, exp, log10, and log.
	tag:
		PRFM_20150920_N27
		0.7.0

2015/9/25
	eval.c
		Fix get macro and init macro performance bug.
        methods.c
		Add File::clear for clear eof state method.
	lib/Stream.prfm
		New class Stream like as File object that process internal input/output.
	tag:
		PRFM_20150925
		0.7.1

2015/10/01
	commands.c / global.[ch]
		Add :silent option to unset and unsets for suppress return original value.
		Fix cstack-release command to release co-routine object.
	cstack.c
		Add stack clear function, cstack_clear and cstack_clear_all.
		Add stack release and clear function, cstack_release_clear.
	methods.c / type.c / commands.c
		Fix auto GC mechanism Coro::next, Coro::release, pause,
		coroutine_handl(internal) and coro_finalizer(internal).
	methods.c
		Add method Coro::stat.
	tag:
		PRFM_20151001
		0.7.2

2015/10/22
	Makefile
		Add -lm option in $(LIB) for memory debuging.
	commands.c
		Fix set command bug that error occured at multipul set.
		Change pause command save previous status.
		Add command uplevel.
	config.h.in
		Fix STACK_SLOT_SIZE comment. (32bit size -> 64bit size)
	lib/pipe.prfm, Pipe.prfm, Stream.prfm
		New feature for command pipe execution.
	lib/cat.prfm, grep.prfm, ls.prfm, util.prfm, ncat.prfm
		pipe command depending.
		Add new command load-func and load-class.
	parse.c
		Add new macro := and ::=.
	setup.prfm
		Change use uplevel command in unknown hook.
	t_gc.h
		Add dummy macro GC_register_finalizer_ignore_self and GC_add_roots.
	types.c
		Add co-routine exception handl.
	methods.c
		Delete file finalizer.

2015/10/23
	Makefile
		Delete HAS_GCACHE flag for bug fix.
	lib/Stream.prfm
		Process optimize Stream::puts by list operation.
		Fix Compatibility File class.
	lib/*
		pipe command depending.
	cstack.c
		Initialize stack head 4096 bytes only at allocate stack.
	setup.prfm, methods.c, commands.c
		Change File I/O newline meaning.

2015/11/15
	eval.c, commands.c, methods.c, interp.c, types.[ch]
		Fix stack trace information collection.
		Fixed functions are:
		  eval_closure(), toy_resolv_var(), toy_push_func_env(),
		  toy_call(), toy_expand(), bind_args(),
		  script_apply_trace_info(), tobe_bind_val().
		Fix script execution bug at apply unknown hook calling.
		Add debug hook calling for debugging suite.
	commands.c
		Add command fork-exec and wait.
	methods.c, commands.c
		Add newline property to File class.
		  At gets() / read:
		  If set-newline is t, newline code is reading.
		  set-newline is nil, newline code is omitted.
		  At puts() / print / println:
		  If set-newline is t, newline code is writing.
		  set-newline is nil, newline code is omitted.
		Add debug command for debugger on/off.
		Add where command for stack getting I/F.
		Fix List::get bug when list is null.
		Fix File class eof condition compativility.
	parser.c
		Fix line detect accuracy.
	lib/exec.prfm
		Add command external process execution.
	lib/debugger.prfm
		Add debugger suite.

2015/11/23
	Makefile
		Delete HAS_GCACHE option.
	commands.c
		Fix fork-exec command, close all file descripter otherwise 0-2.
		Add commands 'true?', 'false?'.
		  'true?' command return if variable is exists and non-nil value true.
		  'false?' command return if variable is not exists or nil value true.
	methods.c
		Add force: option to File::close.
		Add timeout: option to File::ready?.
		Add method File::set-nobuffer.
	eval.c
		Trace output omitted if debug mode is on.
	interp.c
		Add function script_apply_trace_info for String::eval.
	lib/Stream.prfm
		Fix co-routin switch logic.
	lib/debugger.prfm
		Change .n (next) and .s (step) commands to keep mode.
	lib/exec.prfm
		Fix some bugs.
	lib/pipe.prfm
		Fix some bugs.

2015/11/24
	commands.c
		Set FD_CLOEXEC flag to pipe file descripter at fork-exec.
	methods.c
		Set FD_CLOEXEC flag to pipe file descripter at File::open.
		Add File object finalizer.
	lib/exec.prfm
		Fix $stdin eof conditiion.

2015/12/09
	error.h
		Add error 'ErrIOAgain'.
	eval.c
		Add parameter form '(*)' that meaning accept all parameter into variable '$*'.
	lib/Pipe.prfm
		Add terminate hook.
		Add output stream hooking entry 'set-term-hook (h)'.
	lib/Stream.prfm
		Change Stream::puts interface, accept some output arguments.
	lib/debugger.prfm
		Change header print local and object instance variable format.
	lib/exec.prfm
		Add exec sub-process terminate hook.
		Change $OUT(process input) writable watching.
		Change $IN(process output) reading timeout proceed.
	lib/ls.prfm
		Change accept argument otherwise string variable.
	lib/make-proxy.prfm
		Add new utility make-proxy (o) is create a proxy object path throw the $o object.
	lib/pipe.prfm
		Add result returned that last pipe body's last output.
	lib/util.prfm
		Change implement Block::<< and Block::>> using pipe.prfm utility.
	methods.c
		Fix List::item value if item is NULL.
		Fix List::inject.
		Add List::set-car! and List::set-cdr!.
		Add File::set-noblock method and non-blocking I/O option.
		  Return exception ErrIOAgain when if no data stream read.
	setup.prfm
		Add global variable $INFILE and $OUTFILE.
	types.c
		Fix List::string, suppress display '(null address)'.

2015/12/11
	lib/pipe.prfm
		Change syntax error symbol.
	lib/w.prfm
		Add new utility 'w.prfm' that display stack and local variable command.
	methods.c
		Add noblock status to container member.
		Change noblock option _IONBF --> _IOLBF, FreeBSD libraries bug?

2015/12/13
	commands.c
		Add :top option into where command.
	lib/w.prfm
		Fix calling where command with option :top.
	doc/manual.*
		Version 0.8.0.

2015/12/17
	methods.c
		Add Coro::eval.
	interp.c
		Change child interpriter global dictionary is mirrored from
		parent interpriter at new_interp.
	lib/Pipe.prfm
		Change stdin and stdout storege class from instance variable
		to global variable.
	lib/pipe.prfm
		Add parameter variation, if in: is list then do recurcive.
	methods.c
		Fix String::=~ is use global dict '@REGEX_CACHE'.
	setup.prfm
		Add create '@REGEX_CACHE'.
	lib/glob.prfm
		Add glob.prfm like unix glob command.

2015/12/18
	lib/glob.prfm
		Fix some bugs.
	lib/pipe.prfm
		Fix bug when input is list.
	lib/wc.prfm
		Add print INFILE.
	methods.c
		Fix String:split when last character is seperater character bihavior.

2015/12/21
	commands.c
		Change where command returned stack clone object.
	cstack.c
		Change co-routine slot add to GC when allocate time and remove from GC
		when release time.
	methods.c
		Fix Coro::eval exception occured when calling co-routine is done.
		Fix Coro::release exception message.
	doc/*
		Fix 0.8.0_RC3.

2015/12/23
	eval.c
		Fix GETMACRO's left item is SYMBOL.
	lib/util.prfm
		Change unknown receive parameter 'args:' to '*'.
	tag:
		PRFM_20151223_K27
		0.8.0

2015/12/30
	lib/Pipe.prfm
		Change variable move to global INFILE, OUTFILE and CO-MODE.
		Fix refer uninitialized variable.
	lib/exec.prfm, pipe.prfm
		Change check variable CO-MODE value to use true?/false?.
	lib/interp.prfm
		Change set global variable INTERACTIVE.
	perfumesh.c
		Add command parameter '-c "perfume statement"'.
		Change suppress result output when if batch mode.
	setup.prfm
		Fix initial read file name 'setup.toy' to 'setup.prfm'.
	tests/*.res
		Fix no output result by new perfumesh.
	tag:
		PRFM_20151230
		0.8.1

2015/12/31
	lib/pipe.prfm, Pipe.prfm
		Fix don't terminate when last block exited.

2016/01/07
	doc/*
		Add perfumesh command option '-c'.
	setup.prfm, lib/util.prfm
		Add try when load scripts at unknown, load-func and load-class.

2016/01/14
	lib/Pipe.prfm -> lib/PipeWrapper.prfm, lib/pipe.prfm
		Rename class Pipe to PipeWrapper.

2016/02/02
	perfumesh.c
		Fix '-c' option error bug when no commands presents.
	lib/tarai-slow.prfm
		Change lazy parameters expand oparation.

2016/02/15
	methods.c
		Add String::clean, String::upper, String::lower methods.
	lib/http-get.prfm
		Add utility function for simple http access.
	tag:
		PRFM_20160215_A27
		0.9.0

2016/03/02
	lib/exec.prfm
		Fix external process was not runnable condition when return 
		exit status 255.
	lib/http-get.prfm
		Change request URI string.
	lib/lsort.prfm
		Fix list::>> sub-effect bug.
	tag:
		PRFM_20160302
		0.9.1

2016/03/12
	commands.c
		Add count: option in time command.
		Add if command 2nd syntax, new syntax is: if cond true-block false-block.
		Add loop command 2nd syntax, new syntax is: loop do: block.
		Add tag? command.
	lib/util.c
		Add tag utility.
	lib/fib*.prfm
		Fix function terminate condition.
	lib/tarai*.prfm
		Change if syntax, use no keyword parameter.
	tag:
		PRFM_20160312
		0.9.2

2017/01/12
	cell.c, types.c
		Fix performance bug.
	lib/{cat.prfm,grep.prfm,head.prfm,ncat.prfm,wc.prfm}
		Add keyword parameter 'native-value: nv'.
	tag:
		PRFM_20170112
		0.9.3

2017/01/13
	Major change.
		Change Internal character code, char -> wchar_t.

2017/01/16
	parse.c, bulk.c
		Fix parser bug.

2017/01/18
	methods.c
		Add String::uexport and String::uimport! methods.

2017/02/22
	add file
		encoding.[ch]
	methods.c
		Add File::set-encoding, File::set-input-encoding and File::set-output-encoding.
		Add File::init use DEFAULT_FILE_ENCODING.
		Change String::=~ support for UNICODE (internal UTF-32LE code).
	error.h
		Add error ErrNoEncoding.
	bulk.[ch]
		Add unicode encoding at bulk_load_file.
	util.[ch]
		Move function read_size from bulk.c.
	commands.c
		Add unicode encoding at command 'load'.
	config.h.in
		Add global variable defaults, DEFAULT_FILE_ENCODING, DEFAULT_SCRIPT_ENCODING.
	error.h
		Add encoding errors.
	global.[ch]
		Add global variable defaults, DEFAULT_FILE_ENCODING, DEFAULT_SCRIPT_ENCODING.
	perfumesh.c
		Add specify unicode encoding 'RAW' at script loading.

2017/03/01
	encoding.[ch]
		Add encodings are RAW, UTF-8, EUC-JP and Shift-JIS, support character sets are UNICODE,
		JIS X0208 and JIS X0201.

2017/03/03
	commands.c, methods.c
		Fix iterator CTRL_CONTINUE bug.
	lib/diff.prfm
		Add utility diff tool, like UNIX diff command.

2017/03/21
	1.0.0 / PRFM_20170321_OMAPERO15 RELEASE.
	lib/diff.prfm
		Add option :heuristic and :costbase.
	tag:
		PRFM_20170321_OMAPERO15
		1.0.0

2017/06/14
	commands.c
		Fix not return control code bug fix at cond command.
		Add ref command for refer symbol value.
	lib/diff.prfm
		Change costbase algorithm.
	lib/util.prfm
		Add Integer::isprime method.
		Add expr command for RPN expression evaluation.
	methods.c
		Add Integer::nextprime method for search next prime.
		Add Integer::sqrt method for integer square root.
		Add Integer::neg method for integer negate.
		Add Integer::abs method for integer absolute.
		Add Real::neg method for real negate.
		Add Real::abs method for real absolute.
		Add String::fmt control character "%a" for real value show indicate.
	types.c
		Change Real value to_string format.
	tag:
		PRFM_20170614
		1.0.1

2018/04/19
	methods.c
		Add ":string" option in Dict::keys and Dict::pairs,
		return some string type key items when idicated.

2018/05/15
	commands.c
		Fix try command, return CTRL_BREAK when occured CTRL_BREAK in body.
		Fix try command, if occur EXCEPTION at catch body, 
		Add command for TCP/IP socket functions: socket-server, select, accept and close.
		Add connect command option, bind-address: and bind-port:
	lib/util.prfm
		Add function host-address-string. Convert for host-address to string expression.
		Add syntax for like the C-language's for.
	types.h
		Fix comment bug, INTEGER 64bit signed integer -> big integer.
	tests/*
		Fix test scripts and test result file.
	tag:
		PRFM_20180515
		1.1.0

2018/05/17
	methods.c
		Fix Integer::sqrt, Real:sqrt, detect minus number and throw Exception.
	tag:
		PRFM_20180517
		1.1.1

2018/11/21
	lib/chat.prfm,chatd.prfm,chat-config.prfm
		Add new apprication chat system.
	lib/prime-fact.prfm
		A few code optimized.
	methods.c
		Add method File::fd? for query file object holding descriptor number.

2018/12/23
	error.h
		Add new Exception, ErrInfinity and ErrNaN.
	methods.c
		Fix zero divide operation error.
		Add Real::inf? and Real::nan? for check real value infinity and non number value.
		Fix String::split and String::clear wide character bug.
		Fix String::sub when end index supressed bug.
		Add List::create-block.
		Add String::at return specified indexed character.
	commands.c
		Add file sub commands are stat, rm, rmdir, rename, mkdir and chmod.
		Add dict-closure command that return closures dictionary from specified
		blocks environment.
		Add strftime command that format and return string from the specified time-value.
		Add strptime command that date string convert to integer time-value.
		Add time-of-day command that return local machine time.
	lib/util.prfm
		Separate files Block.prfm, Dict.prfm, File.prfm, Integer.prfm, List.prfm,
		Object.prfm, String.prfm, Vector.prfm, consts.prfm, expand-syntax.prfm and
		net.prfm.
		Add Block::>>>, Block::<<<.
		Add String::expand.
	lib/pipe.prfm
		Add :append option for append output to file.
	lib/awk.prfm
		Add awk like function that name is awk.
		Add ll for example file status access.
	parser.c
		Add new escape \<cr> that mean next line to be continued.
		Add octal number literal. ex) 0o[0-7]+
	tag:
		PRFM_20181223_K30
		1.2.0

2019/01/07
	commands.c
		Utility function is moved from commands.c to util.c that name is println.
		Add charactor encoding code to using directory entry name operation commands,
		these names are: load, cd, pwd, file all sub-commands, fork-exec and File::open.
	util.[ch]
		Add new utility functions, encode_dirent and decode_dirent.
	config.h.in, global.[ch], interp.c
		Add global variable DEFAULT_DIRENT_ENCODING. Default value is "UTF-8".
	lib/String.prfm
		Add String -> Integer conversion method, String:<<, String:>>.
	lib/sysinfo.prfm
		Delete output global symbol 'INTERACTIVE'.
	COPYING
		Add year 2019.

2019/01/18
	lib/PipeWrapper.prfm
		Change to co-routin executing within block environment.

2019/02/07
	commands.c
		Chage command 'file stat' use system call stat(2) to lstat(2) and use file type macro.

2019/02/15
	tag:
		PRFM_20190215_A30
		1.2.1

2019/02/26
	commands.c
		Add 'symbolic-link' property to file stat result list, when 'type' property is "l".
	lib/ll.prfm
		Add display symbolic link object.
	tag:
		PRFM_20190226
		1.2.2

2019/04/01
	methods.c, array.c
		Fix for vector item only cloning at Vector::set time.
		Fix for dict item only cloning at Dict::set time.
	eval.c
		Fix primitive type (Integer and String) only cloning at
		given function call parameter.
	commands.c
		Fix primitive type (Integer and String) only cloning at
		called set command.
	types.c
		Rewrite toy_clone.
		Primitive data types are Integer and String in perfume language.
	lib/Integer.prfm
		Add Integer method, Integer::C and Integer::P.
	lib/awk.prfm, ll.prfm
		Change parameter order and some files and use standard input when
		no file name specified.
	lib/prime-fact.prfm
		Fix a few performance.
	tests/*
		Fix bug and some tests added.
	tag:
		PRFM_20190401
		1.2.3

2019/05/27
	lib/standard-io-command-templ.prfm
		Add file lib/standard-io-command-templ.prfm as for standard I/O
		command template function.
	lib/{awk.prfm,cat.prfm,grep.prfm,head.prfm,ncat.prfm,wc.prfm}
		Change these commands are change to use standard-io-command-tmpl.
	eval.c
		Fix, if statement first item is premitive then clone it.
	tag:
		PRFM_20190527
		1.2.4

2019/06/17
	lib/PipeWrapper.prfm
		Fix bug, choosing select co-routine's local stack.
	lib/expand-syntax.prfm
		Add function for pipe utility command suite are:
		  List::@>{body}
		  	Input from list and list item output to $stdout.
		  >@{body}
		  	Input from $stdin and iterative process body, only last result
			to $stdout.
		  >@>{body}
		  	Input from $stdin and iterative process body and output	result
			to $stdout per call body.
	methods.c
		Fix Integer::++ and Integer::-- function argument can acceptable any type
		(auto conversion to Integer call).
	tag:
		PRFM_20190617
		1.2.5

2019/06/25
	commands.c
		Add native functions argspec description.
		Add new command 'argspec' for get the specified functions or native commands
		argument spec list.
		Change case command syntax, if no matches pattern then select 'default:'
		keyword body.
	methods.c
		Add native methods argspec description.
	lib/*
		Change case command default body specify.
	types.c
		Change '<NATIVE>' objects printable style.
	tag:
		PRFM_20190625
		1.2.6

2019/08/23
	commands.c
		Add new command spawn is execute unix command. Standard I/O descriptores are
		inherit to child process.
	tag:
		PRFM_20190823
		1.2.7

2019/10/16
	eval.c, parser.c
		Fix Object::init given parameter specific bug.
	hash.c
		Add Hash bucket size const.
	methods.c, array.[ch], lib/Dict.prfm, lib/Vector.prfm
		Add Vector::resize method.
		Add Vector::fill method.
		Add Object::literal method.
		Add Dict::stat method.
		Some error messages are fixed.
	lib/List.prfm
		Add List::max, List::min methods.
	lib/String.prfm
		Add String::replace method.
	tag:
		PRFM_20191016
		1.2.8

2019/12/02
	lib/interp.prfm
		If interpriter input line is continued, insert "\n" to command string.
	lib/String.prfm
		Add String methods String::!, String::abs and String::neg.
	commands.c
		and/or commands are proceed argument if it was block type object.

2020/02/19
	commands.c
		Add command 'control?' for return t if object type is control.
	eval.c
		If return control object from commands or functions, return thru it
		expect control#return.
	lib/List.prfm
		Add methods List::permutations and List::combinations.
	lib/date.prfm,edit.prfm
		Add utilitys date and edit.
	lib/interp.prfm
		If return control object from command line, display this after convert to string.
	lib/sysinfo.prfm
		Add display item 'DEFAULT_DIRENT_ENCODING'.

2020/02/21
	Makefile, binbulk.[ch], commands.c, methods.c, interp.c, setup.prfm, global.[ch],
	util.[ch], eval.c, types.[ch], toy.h, lib/util.prfm, lib/Bulk.prfm
		Add new data type Bulk for operation binary data and file i/o.
		Add class Bulk.
		Add command 'bulk' for create Bulk data object.
		Add methods Bulk::append, Bulk::get, Bulk::set, Bulk::seek, Bulk::position,
		    Bulk::len, Bulk::capacity, Bulk::truncate, Bulk::read and Bulk::write.
	lib/hexdump.prfm
		Add utility hexdump.
	tag:
		PRFM_20200221
		1.3.0

2020/03/03
	lib/hexdump.prfm
		Add display character map.

2020/03/11
	*
		Delete data type NIL.
		Add data type BOOL.
		't' meaning boolean true and 'nil' meaning boolean false.
	lib/Object.prfm
		Add method Object::??, like a null coalescing operator.

2020/03/12
	*
		Change boolean literal expression: true was change t to <t> and	false was change nil to <nil>.
	commands.c
		Add command bool?.
	lib/Object.prfm
		Add method Object::???, 3 operand operator.

2020/03/18
	binbulk.[ch]
		Add functions binbulk_base64_encode, binbulk_base64_decode and binbulk_is_eof.
	methods.c
		Add methods Bulk::base64encode and Bulk::base64decode.
	lib/base64.prfm
		Add utility base64.prfm.

2020/04/15
	doc/manual.*
		All fonts are changed to Noto-* font family.
	tag:
		PRFM_20200415
		1.4.0

2020/05/28
	lib/flat.prfm
		Fix OBJECT type objects method collection algorithm.

2020/09/10
	type.h, commands.c
		Add description string in CONTAINER data type structure.
		Add self function object pointer in Toy_Type structure.
		Add command self-func.
	methods.c
		Add method Object::hash.
	cstack.[ch]
		Add co-routin slot status SS_PEND.
		This status indicates that the co-routine is between the end and the release.
	methods.c
		Add new method List::concat! and change side effects disappeared from List::concat.
		Add new method Container::desc.
	lib/{chat.prfm, chatd.prfm}
		Change algorithm.
	lib/expand-symtax.prfm
		Fix load-func and load-class when expect error in loading file.
	lib/interp.prfm
		Change to display stacktrace when execute line occurds exception.
	ncurses.c
		Add optional feature, curses library wrapper commands.
	tag:
		PRFM_20200910
		1.4.1

2020/10/27
	Makefile
		Separate option item.
	bulk.[ch]
		Add parse source text detect new-line character including '\r'.
	commands.c
		Delete signal handling symbol 'SIGALRM'.
		Add ':notrace' option at load command, if specified :notrace not make the
		    trace information.
		Adjust where command output for stack-trace information and
		    delete ':top' option.
		Add commands set-itimer and enable-itimer, for interpriter can recieve the
		    SIGALRM signal at interval. 
		    If interpriter recieve a SIGALRM, resume the co-routine myself.
		Change while condition need specified closure block.
	config.h.in
		Change default STACK_SLOT_MAX 1024 -> 256.
	cstack.[ch]
		Add utility API name is cstack_get_safe_addr.
	eval.c
		Add occurs the exception when C stack approaches the barrier address in stack slot.
		Add co-routine resumed myself when recieve a SIGALRM and set enable-itimer, set-itimer.
		Adjust making trace-info for debugger.
		Add object name in exception message when 'No such method' occured.
		Delete signal SIGALRM handling.
	interp.c
		Adjust output stack-trace information at utility API name is get_stack_trace output.
	perfumesh.c
		Fix use load command for read the first script. 
	lib/Stream.prfm
		Add Stream::set-itemlimit and add limit for output item by 'Stream::puts :nonewline' counts.
	New added pmacs text editor suite.
		Add pmacs text editor suite.
		New files are (in under lib/): BufferBase.prfm, BufferGeom.prfm, BufferManager.prfm,
			CombiWindow.key, CombiWindow.prfm, EditBuffer.prfm,
			EditorCommandDefs.prfm, MiniWindow.prfm, ScratchBuffer.prfm,
			WUtil.prfm, Window.prfm, WindowBase.prfm, color-defs.prfm,
			pmacs.conf, pmacs.prfm, pmacs.files, pmacs.TODO, Mode-BufferList.key,
			Mode-C.key, Mode-Interp.key, Mode-Prfm.key, Mode-Text.key, pinterp.prfm

2020/12/09
	Makefile, pkg/*
		Add package installer maker.
	commands.c, types.h
		Fix set-itimer signal stack use for static area.
		Add command atomic.
	eval.c
		Add signal masking in atomic block.
	perfumesh.c, interp.c
		Add perfumesh command option -D PREFIX for specified system top directory.
	methods.c
		Add commands String::alphabetic?, String::numeric? and String::alphanumeric?.
	tag:
		PRFM_20201209
		1.5.0_RC_1

2020/12/23
	Makefile
		Fix build-pkg and install file leaked.
	lib/EditorCommandDefs.prfm
		Add editor comman trace-editor-command.
		Add editor commans replace-buffer and replace-region.
		Change open-file, save-buffer, save-buffer-as initial value
		  to previous buffer's directory presents.
		Chagen open-file, save-buffer, save-buffer-as input "***//" meaning
		  to specify root directory and replace to "/".
		Change key-binding-list to display key description and all command list.
		Add editor commands are replace-buffer and replace-region.
	lib/CombiWindow.{prfm,key}
		Add setkeydesc and key description.
	lib/EditBuffer.prfm
		Change do save-contents UndoBuffer and RedoBuffer not clean.
		Add replace-buffer and replace-region API.
	lib/ScratchBuffer.prfm
		Change initial screen message.
	lib/String.prfm
		Add String::replace-return-times for return replace string and 
		  replaced times list.
	lib/WUtil.prfm
		Add functions are open-file and get-current-buffer-dir.
	lib/pmacs.keymap
		Add FreeBSD's terminal keymaps.
	lib/pmacs.prfm
		Add parse argments for specified initial open files.
	perfumesh.c
		Fix arguments parase bug, for specified initial open files.
	tag:
		PRFM_20201223_K32
		1.5.0

2020/12/25
	pmacs update:
		Change editor command split-* and Window.prfm, to redraw window unuse KEY_RESIZE key event.

2020/12/29
	pmacs update:
		Add theme MONO, MONO-GREEN, BLUE-BACK and RETRO.
		Add editor command set-theme.
		Add editor command set-mode.
		Change file name color-defs.prfm -> theme-color.prfm.
		Add Window::add-background specify after: msec property mechanism
		  that execute body after passed time msec.
	commands.c
		Add :msec option to time-of-day, for retun clock by mili-sec.

2020/12/30
	lib/Block.prfm
		Add Block::redirect-out method.
	pmacs update:
		Add BufferManager::find-buffer method.
		Add CombiWindow:: cursor-fit-to-top method.
		Add editor commands are grep, grep-next and grep-previous,
		  key bind is KEY_F5 -> grep-next, KEY_F6 -> grep-previous.
		Add Grep mode and mode file 'Mode-Grep.key'.
		Change editor command search key bind CTRL_V to use paste buffer.
		Change LIGHT theme mini window color.

2020/12/31
	pmacs update:
		Add editor commands are fit-to-top and fit-to-bottom.
		Add editor command is select-region-all.
		Add editor command is read-grep-file.
		Add mini input key bind CTRL+{KEY_RIGHT,LEFT} to line buffer move top or bottom.

2021/01/02
	pmacs update:
		Add editor commands are:
		  select-region-paragraph, move-screen-top, move-screen-bottom,
		  move-next-paragraph-fit-to-top, fit-previous-paragraph-fit-to-top.
		Add methods are CombiWindow::move-screen-top and CombiWindow::move-screen-bottom.
		Fix EditorCommandDefs.prfm coding style.
		Add execute-editor-command key bind CTRL_V is paste command from copy-buffer.
		Add trace-editor-command key bind CTRL_V is paste command from copy-buffer.
		Fix grep, grep-next, grep-previous: window re-build optimized, if "Window#1" and
		  "Window#2" is alive then don't re-build these window.
		Add sample editor command mini-move-message.
		Change LIGHT theme color.

2021/01/03
	COPYING
		Add year 2021.
	pmacs update:
		Add new key binds:
		  F14 -> split-vertical-window-3
		  F15 -> split-horizontal-window-3
		  F26 -> split-vertical-window-4
		  F27 -> split-horizontal-window-4
		Add key maps:
		  KEY_F73 -- KEY_F96,
		  kIC6, kIC7, KIC8, kDC6, kDC7, kDC8,
		  kHOM6, kHOM7, kHOM8, kEND6, kEND7, kEND8,
		  kPRV6, kPRV7, kPRV8, kNXT6, kNXT7, kNXT8,
		  kDN8, kUP8, kRIT8, kLFT8.
		Change CombiWindow::display-evaluation and editor command eval-region
		  set mark when enter command (CTRL_J).
		Add editor commands:
		  roll-current-window-backward, 
		  next-page-anothor-window, previous-page-anothor-window, goto, cd,
		  previous-smooth-scroll-down-anothor-window, previous-smooth-scroll-up-anothor-window,
		  delete-word, delete-to-end-of-line.
		Change grep-next and grep-previous use CWD buffer line when exists this
		  record, open target file before change directory it.
	tag:
		PRFM_20210104
		1.5.1

2021/01/23
	pmacs update:
		Add new editor commands:
		  search-next, search-previous, insert-newline,
		  set-protect, no-protect,
		  pwiki-next-link, pwiki-previous-link, pwiki-click-link,
		  pwiki-link-back.
		Change key descriptor string and display binding-list format.
		Add edit buffer protect mode and display window status line this.
		Add PWiki mode.
		Fix jumb-buffer detect buffer name from title line.
		Add previous buffer name to EditBuffer at pwiki-click-link
		 and be able to return previous buffer by pwiki-link-back command.
		Add auto mkdir pwiki document directory.
	commands.c
		Fix 'file mkdir' help message.

2021/01/27
	pmacs update:
		Change fit-to-cursor line limit, top 2->1, bottom 2->1.
		Change smooth-scroll-up/smooth-scroll-down line count 2 -> 1.
		Change smooth-scroll-right/smooth-scroll-left column count 2 -> 4.

2021/01/28
	lib/man.prfm
		Add new utility for show man pages in interp.
	commands.c
		Fix file command help message.
	pmacs update:
		Add key bindings, F09 -> no-protect, F21 -> set-protect.
		Add EditBuffer if open content file is read-only file, set buffer to protect.
		Add EditBuffer if buffer protect at save content, return error to editor command.
	ncurses.c
		Add curs-col command for man page line arrangement utility.

2021/02/08
	util.[ch], commands.c, methods.c
		Fix if return UTF-8 string from strerror(3), decode to unicode and return to caller.
	ncurses.c
		Add some character to arrangement at curs-col.
	pmacs update:
		Add show-message at set-protect and no-protect.

2021/02/09
	pmacs update:
		Change Interp line length limit by value $INTERP_LIMIT_LEN.
		Add set-line-limit editor command.
		Add man reading suite. New editor commands are:
		  man, man-click-link, man-link-back, man-next-link and man-previous-link.
		Add Man mode and Mode-Man.key file.
		Add compile editor command.

2021/02/10
	pmacs update:
		Change if buffer is protected key-in is string but go to key binding mode.
		Add Man mode key binding " " is next-page, "b" is previous-page and
		  KEY_BACKSPACE is man-link-back.
		Change default INTERP_LIMIT_LEN value 80 -> 128.
	methods.c
		Fix String::{alphabetic?|numeric?|alphanumeric?} if it's "" return <nil>.

2021/02/11
	commands.c, eval.c
		Add command eval-stat for statistics evaluator count.
	Makefile
		Add compile option EVAL_STAT.

2021/02/13
	ncurses.c
		Add key repeat value on curs-keyin returns.
	pmacs update:
		Add key move acceleration when up/down key repeat.

2021/02/15
	pmacs update:
		Change key binding at mode pwiki and man, *-link-back assign to KEY_BTAB.
		Change to pwiki link and man link regex defined at pmacs.conf.
		Fix insert character reverse execution (ex. undo) when cursor at buffers bottom,
		  add insert backdelete-character-at to undo buffer before.
		Add line-insert interface at EditBuffer.
		Change to use EditBuffer::line-insert editor commands is:
		  replace-buffer, replace-region, smart-indent, smart-dedent.
		Change coding style use RQuote '\' at line tail.
		Change key repeat factor caluculate algorithm.
	parser.c
		Add RQuote can use '\' character at line tail.

2021/02/17
	pmacs update:
		Change Editor command no-protect -> set-noprotect.

2021/02/19
	pmacs update:
		Add help editor command.

2021/02/22
	pmacs update:
		Add editor command view-file for file open for protect mode.

2021/02/27
	pmacs update:
		Add PWiki document link form, execute editor command when if link 1st character is '&'.
		Change start message at ScratchBuffer.
		Fix key description, Ctrl+| -> Ctrl+\.

2021/03/05
	pmacs update:
		Add editor command 'shell' for run shell under pmacs sub-prosess.

2021/03/07
	pmacs update:
		Add editor commands are: git-pull, git-push and git-commit.

2021/03/11
	pmacs update:
		Add editor command read-file for read file and insert into buffer.
		Change start log centering.

2021/03/12
	pmacs update:
		Add mark at editor command search-next and search-previous.
		Change search command split to search-forward and search-backward.
		  search-previous bind to Ctrl+R.
	tag:
		PRFM_20210312
		1.5.2

2021/03/15
	pmacs update:
		Fix editor command completion bug at execute-editor-command and trace-editor-command.
		Add mark at goto editor command.
		Add grep command become to use region selection.
		WUtil::line-input a few fix.

2021/03/16
	pmacs update:
		Add new property LastRaiseTimestamp at BufferBase for buffer list ordering.
		Add BufferManager::get-buffer-list-order-by-newer for buffer list ordering newer.
		Change refresh-screen editor command for re-construct screen.
		Change key-binding-list formatting.
		Change buffer-list list order by last raise newer.
		Add mode Java and mode file Mode-Java.key.
		Change select-region-paragraph become any cursor position select region.

2021/03/17
	pmacs update:
		Add window status and vertical line color change if buffer mode is protect.
		Change HILIGHT_REGEX_DEFAULT regex list style.
		Add pmacs.conf description.

2021/04/09
	pmacs update:
		Add editor command cursor-{up|down|right|left}-with-region and
		  assign to Shift+Ctrl+{up|down|right|left} key.
		Change algorithm smart-newline, smart-indent and smart-dedent.
		Fix grep, read-grep-file and compile set cursor point.
	parser.c
		Add newline character if detect '\r' at parse string and rquote.
	tag:
		PRFM_20210409
		1.5.3

2021/04/19
	pmacs update:
		Add set-line-limit command become accept 'auto' value that mean line folding by
		  current screen width.
		Fix CombiWindow::get-region if EditBuffer size truncated less than region size,
		  region hilight fit to buffer size.
		Add if editor command paste-region execute in region select, do paste buffer after
		  delete region.
		Change in pmacs.conf value INTERP_LINE_LIMIT set default to "auto". 

2021/04/20
	pmacs update:
		Fix dismis the cursor flicker at move cursor continuously.
		Fix execute-editor-command and trace-editor-command if null regex input become
		  no make command list.
		Add key bind to Man mode, 'j' is smooth-scroll-down and 'k' is smooth-scroll-up.
	ncurses.c
		Add curs-set command for cursor on/off.

2021/04/21
	pmacs update:
		Change keyboard repeat accelaration value parameterized.
		Add editor command samples guru-guru and guru-wave.

2021/04/22
	pmacs update:
		Change starting logo display position (base width/hight value change).
		Fix mini buffer refresh when screen refreshed.
		Change man editor command read region if selected the buffer region.

2021/04/29
	commands.c
		Change atomic command becomes nested call safe.
	ncurses.c
		Change delete window struct memory if window destroyed.
	pmacs update:
		Define color pair name to ColorDB "fg-color"."bg-color" all combination.

2021/05/10
	pmacs update:
		Add auto insert tabs when privious line end character is ':' at smart-newline.

2021/05/14
	lib/awk.prfm
		Fix clear $1 .. $9 variables when next line iteration and if item count less than 9,
		  then create $(item_count+1) to $9 variables.
	tag:
		PRFM_20210526
		1.5.4

2021/05/27
	pmacs update:
		Change buffer select order after buffer killed.
		Change set no modifiy to buffer when do undo but undo buffer is empty.
		Change pmacs.files add to IndependentWindow.prfm and IndependentWindowManager.prfm
		  for window style menu implementation.
	misc/Tarai.vim
		Add example vim-scripts tarai function.

2021/06/02
	pmacs update:
		Fix curs-render-line is return exception if render line including corrupt unicode
		  character, CombiWindow::render become catch the exception.
		Add visible bell in show-message with :bell option, some editor commands become
		  use this option.
		Fix input-character-code editor command if input unicode code point greater than
		  0x1fffff become error.
		Fix pwiki-click-link if document link name begin '/' document content-path is
		  same as link name.
	ncurses.c
		Change double width character range to greater than 0x10ff.

2021/06/03
	pmacs update:
		Fix grep command restore the current directory after grep execute.
		Fix grep-next and grep-previous are read CWD *Grep* buffer line
		  if grep result path not start '/' character.
		Change *Scratch* buffer starting message and pinterp starting message.
		Fix to no message show at exit timming if pmacs don't have some modified buffer.
		Add quick start document 'lib/pdoc/00quick-start.pwiki' file.

2021/06/10
	pmacs update:
		Add blink: option at curs-keyin function. If blink specified <nil> system blink, 
		  specified <t> Pmacs curs-keyin built-in blink.
	tag:
		PRFM_20210610
		1.5.5

2021/06/14
	pmacs update:
		Fix buffer select order after buffer killed.

2021/06/15
	methods.c
		Add :text option to String::=~ for search AS IS string.
	pmacs update:
		Add text search mode, if toggle regex/text mode do type CTRL+T in search mode.
		Add -r flag in grep command initial prompt.
		Fix file selector matched regex become only matchies input file name.

2021/06/16
	pmacs update:
		Change to ignore case at search command text mode.

2021/06/17
	ncurses.c
		Change the font display width calculation to call the C library wcwidth(3).

2021/06/18
	ncurses.c
		Change font for unknown and combining character at rendering modules.

2021/06/19
	pmacs update:
		Add show-font editor command.
		Change select buffer order at buffer list.
	lib/prime-regex.prfm
		Add new sample for compute prime number by regex.
	lib/show-all-character.prfm
		Add new utility for output all character code.
	tag:
		PRFM_20210619
		1.5.6

2021/06/24
	pmacs update:
		Add use EMERGENCY_SHELL config parameter at 'pmacs.conf' if DEFAULT_SHELL is failed.

2021/07/01
	Makefile, config.h.in, global.[ch], interp.c
		Add 'BUILD' configuration parameter in config.h.in.
	lib/sysinfo.prfm
		Add display BUILD variable.
	pmacs update:
		Add display version and build at pinterp started.

2021/07/02
	pmacs update:
		Add ALT+[alphabetic-char] combination key assign,
		  Occures key code following this:
			ALT+a	->	ALT_a (symbol)
			ALT+A	->	ALT_A (symbol)
			ALT+SHIFT+!  -> ALT_exclamation (symbol)
		  More information see pmacs.keymap and CombiWindow.key.
		Add editor commands are:
		  show-keycode - display input key code, key map (if exists) and binding command.
		  next-buffer-another-window - switches a anothor window buffer forward.
		  previous-buffer-another-window - switches a anothor window buffer backward.

2021/07/04
	ncurses.c
		Change unknown character font display width change 2 from 1 (selectable, see comment).

2021/07/08
	tag:
		PRFM_20210708
		1.5.7

2021/07/09
	pmacs update:
		Fix "EditorCommandDefs.prfm" some editor commands when EditBuffer::get-buffer-line returns <nil>.

2021/07/12
	pmacs update:
		Add keymap ALT+" " as ALT_space.

2021/07/15
	pmacs update:
		Fix search-next / search-previous, use SEARCH_MODE (value: text or regex) variablle thats indicate
		  previous searched keyword type. If keyword type is regex use get-search-{next|previous},
		  if type is text use get-search-{next|previous}-text function.

2021/07/26
	pmacs update:
		Change config parameter BLINK in pmacs.conf default value set to <t>.

2021/08/02
	pmacs update:
		Add editor commands are: window-grow-right, window-grow-left, window-grow-upper and window-grow-lower.
		  These commands are move window split line to left/right, down/up.
		Add editor command is window-split-ratio-reset.
		Add key bindngs are: 
		  ALT_h -> window-grow-right
		  ALT_l -> window-grow-left
		  ALT_h -> window-grow-upper
		  ALT_k -> window-grow-lower
		  ALT_o -> window-split-ratio-reset

2021/08/08
	pmacs update:
		Change editor commands are: window-grow-* use DIV_RATIO_RES variable.
		Change Window::*-split-*-sub are: use MIN_WINDOW_{WIDTH,HEIGHT} variable.
		Add configure variable MIN_WINDOW_WIDTH, MIN_WINDOW_HEIGHT, 
		  DIV_RATIO_RANGE_{MIN,MAX}, DIV_RATIO_RES, DIV_RATIO_{MIN,MAX}.
	tag:
		PRFM_20210808
		1.5.8

2021/08/19
	pmacs update:
		Change if standard input channel is ready, skip display refresh.
		Fix push incorrect data to undo-list when cursor over the EOF mark.

2021/08/20
	pmacs update:
		Change terminate mini-input-mode when recieve window resize in mini-input-mode.

2021/08/27
	eval.c
		Fix incorrect trace information output in stack-trace.
	pmacs update:
		Add active mini window color name is ACTIVE_MINI_WINDOW,
		  if start mini input mode use this.

2021/08/29
	eval.c
		Delete trace information copy.

2021/08/30
	ptrace.prfm
		Add trace command for running under pmacs editor.
	pmacs update:
		Change smart-move-forward and smart-move-backward command is move cursor
		  to word boundary but if found their character '(', '[', '{',
		  ')', ']' and '}', stop at it.

2021/09/01
	pmacs update:
		Fix smart-move-backward command bug.
		Change *Scratch* buffer display stdout and stderr display order,
		  new order is stdout -> stderr.
		Add new utility function vector::get-next-pos (x y) in WUtil.prfm file.

2021/09/02
	pmacs update:
		Add detect function that another process modify same file
		  at save-buffer editor command.

2021/09/09
	pmacs update:
		Change bind the window #1's buffer to window #2/#3/#4, if window split
		  first time.

2021/09/12
	pmacs update:
		Add new feature:
		  * New buffer 'DiredBuffer'.
		  * New editor commands - dired, dired-open, dired-link-back, dired-cursor-up,
		    dired-cursor-down, dired-sort-by-{name,time,size}-{ascending,descending} and
		    dired-filter.
		  * New mode 'Dired' and mode file 'Mode-Dired.key'.
		  * New color defs DIRED_* in theme-color.prfm.
		Change open-file, view-file if specified a directory at mini-input, open dired.
		Fix WUtile::get-current-buffer-dir.

2021/09/13
	pmacs update:
		Add EditBuffer::get-content-dir for buffer's content path convert to
		  content directory.
		Add ScratchBuffer::get-content-dir for return pinterp current directory.
		Change editor command grep, use initial grep directory get from buffer's
		  content directory path use get-content-dir method.
		Change file name lib/theme-color.prfm to lib/pmacs-theme.prfm.
		Change mini-input fg/bg color.
		Change pmacs.prfm coding style.

2021/09/14
	pmacs update:
		Change dired sym link display format.
		Change man buffer name '*Man xxx*' -> 'Man=xxx'.

2021/09/20
	pmacs update:
		Change delay command output result under pinterp, if Stream item-count is not full.
		Fix cursor move to bottom of buffer when pinterp output result.
	tag:
		PRFM_20210920_N33
		1.5.9

2021/10/19
	pmacs update:
		Change key-binding-list format a few.
		Add WindowBase::set-color method.
		Change show-message messages color use MINI_CAUTION if set parameter 'bell:'.

2021/11/02
	pmacs update:
		Fix start logo display buf when window width less than logo width.

2021/11/11
	pmacs update:
		Change EditBuffer::init construct same as called init-constructor :init-only result.
	tag: 
		PRFM_20211111
		1.5.10

2021/11/12
	pmacs update:
		Change window vertical bar move LEFT to RIGHT default.
		Add color name INTERP_PROMPT_HILIGHT.

2021/11/26
	lib/find.prfm
		Add new find utility, like the UNIX find command.

2021/12/21
	methods.c, types.c
		Fix auto release co-routines C-stack if coro script was done.
		Fix Coro::next re-set parent interp object into coro object by call.
	pmacs update:
		Fix editor command dired-open, if get-path return <nil> use current directory path.

2021/12/22
	pmacs update:
		Add buffer select using for shortcut key in buffer-list command.
		Add buffer delete using for "d" key in buffer-list command.
		Add key bind in BufferList mode, "0"-"9" (select buffer), "d" (buffer delete).

2021/12/23
	tag:
		PRFM_20211223_K33
		1.5.11

2021/12/24
	pmacs update:
		Add curs-keyin's parameter blink-fact: value is cursor blink iteration fact.
		Add global BLINK_FACT, initial value to 4.
		Change INPUT_TIMEOUT 50 -> 20, for resolutions to less.
		Change INTERP_ITIMER_INTERVAL 125 -> 50.
	ncurses.c
		Add new param blink-fact: in func_curses_keyin.

2022/01/12
	pmacs update:
		Fix EditBuffer initial content-path value setup at constructor by current
		  directory.
		Add *Grep* buffer content-path value.
		Tuning key-in timing value, INPUT_TIMEOUT, BLINK_FACTk, KEY_REPEAT_ACCEL_DELAY,
		  KEY_REPEAT_ACCEL_MAXLINE, BLINK_FACT.
2022/01/29
	pmacs update:
		Fix content-path illegal initial value set and change get-content-dir return 
		  to current directory if dir is not set.

2022/02/01
	eval.c, interp.c
		Optimizing script type ex) prim-val ops args... pattern, eval.c omitted classes
		  dictionary access for prim-val's class resolving.

2022/02/03
	tag:
		PRFM_20220203
		1.5.12

2022/02/09
	methods.c
		Add String::fmt "%w" new indicator, compute value length as console display column width.
		Add String::display-width for compute console display column width.
	pmacs update:
		Change buffer-list use new format indicator "%w" at buffer name and content-path display.
		Change *Directory* buffer use new format indicator "%w" at file display.
		Change mini-input compute cursor indexing use String::display-width.

2022/02/10
	pmacs update:
		Fix mini-input compute cursor indexing bug.

2022/02/13
	commands.c
		Add set-locale command for set locale LC_ALL.
	pmacs update:
		Change use set-locale command at pmacs initialized.
	tag:
		PRFM_20220213
		1.5.13

2022/02/14
	encoding.[ch]
		Add new encoding UTF-8F, if file has bad utf-8 sequence then replace bad character to 
		  fake character(u+2592).
	methods.c
		Add File stat new item 'omit-cr', it's means if charidge return in file read line omit it.
		Add File stat new item 'include-cr', if charidge return included in file indicate <t>.
	lib/exec.prfm
		Add set omit-cr to <t> in $IN and $ERR file stat.
	pmacs update:
		Add new config variable DEFAULT_EDIT_ENCODING = UTF-8F. If read binary file but not 
		  occured error.
		Add new config variable FILE_OMIT_CR for Windows type text reading.
		Add indicator DOS type file at window status line.
		Add indicator DOS type file at *Buffer List*.
		Add EditBuffer::save-content write '\r' character if DOS type file read before.
	tag:
		PRFM_20220214
		1.5.14

2022/02/15
	methods.c
		Fix File::gets '\r' and omit-cr option is <t> and next char is not '\n', 
		  then read and append result string '\r'.
	pmacs update:
		Add new key bind CTRL_G to goto command.
	tag:
		PRFM_20220215_A33
		1.5.15

2022/02/16
	encoding.[ch]
		Fix encorder UTF-8F, if read bad UTF-8 sequence, return error info and read line both.
	methods.c
		Add error indicator to File::stat, property name is encode-error. 
	pmacs update:
		Add editor command set-encoding for select encoding and re-load the file.
		Add EditBuffer::set-encoding, EditBuffer::get-encoding and EditBuffer::reload-content method.
		Fix if encorder return UTF-8 bad sequence, set protect to buffer.
		Add indicator current encoding at window status line.
		Add indicator current encoding at *Buffer List*.
		Add new key binding ALT_e to set-encoder command.
	tag:
		PRFM_20220216
		1.5.16

2022/02/18
	encoding.[ch]
		Add decorder error indicat to error_info, Shift-JIS and EUC-JP.
	pmacs update:
		Add new editor command dismiss-current-window.
		Add key binding Shift+F01 to dismiss-current-window.
		Change ACTIVE_STATUS_LINE_PROTECT LIGHT theme color.
	tag:
		PRFM_20220218
		1.5.17

2022/02/28
	pmacs update:
		Change indicate DOS type flag position at window status line.
		Change indicate DOS type flag position at *Buffer List*.
		Add indicate encoding error at EditBuffer method name is 'enc-error?'.
		Change EditorCommandDefs::dired-open call DiredBuffer::update if buffer
		  is already exists.
		Change if file encoding error occured, show error message at some editor commands.
		Change *Scratch* buffer initial text.
	tag:
		PRFM_20220228
		1.5.18

2022/03/01
	pmacs update:
		Add editor command set-tab-width for global set TAB display width.

2022/03/10
	pmacs update:
		Change 00quick-start.pwiki documentation a few.
	tag:
		PRFM_20220310
		1.5.19

2022/03/11
	pmacs update:
		Change grep command, append "/" to initial directory path tail.
		Fix EditBuffer Encoding instance variable setting.
		Change BLINK_FACT 4 -> 3.

2022/03/14
	doc/manual.pdf
		Update Perfume documentation for version 1.5.19.
	tag:
		PRFM_20220314
		1.5.20

2022/03/28
	methods.c
		Change File property name omit_cr -> ignore_cr, change method set-omit-cr -> set-ignore-cr.
	pmacs update:
		Add config variable TOP_OF_STATUS_BAR for display status line at window top.
		Add global setting variable STDIO_FILE_ENCODING for standard-io-command-templ.prfm specify file encoding.

2022/03/29
	pmacs update:
		Add new editor command internal-stat for display system internal status
		  at *Scratch* buffer.
		Add key bind Ctrl+Alt+F12 to internal-stat.
		Change kill-interp for new create stdin/stdout/stderr object.

2022/03/30
	pmacs update:
		Fix *Directory* buffer leak.
		Change buffer-list buffer name display fix length to fit to max buffer name length.
2022/04/01
	tag:
		PRFM_20220401
		1.5.21

2022/04/06
	pmacs update:
		Change internal-stat show w(where command) display to wide.
		Change mode Interp HILIGHT_REGEXP.

2022/04/14
	pmacs update:
		Add editor command buffer-list-completion and bind to CTRL+B at BufferList mode.

2022/04/15
	tag:
		PRFM_20220415
		1.5.22

2022/04/16
	Makefile, eval.c
		If you specified NO_LAZY_CALL=yes at Makefile, the lazy call mechanism when calling a function is omitted.
	pmacs update:
		Add mode-select function and global variable MODE_SELECT_LIST defined at pmacs.conf
		  and delete MODE_SELECT closure.
		  If you want customize mode select, you can append mode select logic to $MODE_SELECT_LIST.

2022/04/18
	pmacs update:
		Add user configuration feature.
		  Add WUtil::read-user-conf-file is read the user configuration file.
		  Read $HOME/.pmacs/pmacs.conf at startup if exists.
		  Read $HOME/.pmacs/default.key at CombiWindow re-builded if exists.
		  Read $HOME/.pmacs/Mode-[mode].key at set-mode if exists.
		Fix WUtil::show-message if $MainWindow not exists occured error.

2022/04/19
	util.c
		Fix load command faild if file size is zero.
	tag:
		PRFM_20220419
		1.5.23

2022/04/23
	pmacs update:
		Change raise buffer order when previous buffer killed.
		Change DARK theme color.

2022/04/25
	pmacs update:
		Fix smart-move-forward cursor jump to illegal position when cursor on EOF.

2022/04/28
	pmacs update:
		Add spot color face function.

2022/05/05
	tag:
		PRFM_20220505
		1.5.24

2022/05/15
	methods.c
		Add if data stream stoped but not line terminate when File::noblock setting,
		  File::gets returns data early exit to caller.
		  This case the File object property set early-exit flag to <t>.
		Add method File::set-readbuffer-max if set this (no zero), return data to caller
		  when if input data length greater than set-readbuffer-max value.
		Add flag value File::early-exit, sets if File::gets input data greater than
		  File::set-readbuffer-max or File::noblock is blocked.
	util.[ch]
		Add new utility function is_read_ready.
	lib/exec.prfm
		Change set to property methods File::set-readbuffer-max and File::set-noblock,
		  and use condition flag File::early-exit.

2022/05/17
	commands.c
		Add fork-exec option :pty, this option is use pty device for communication to 
		  the child process.
		Add fork-exec to setup process group when specify :pty option.
	methods.c
		Add File::noblock case to File::ready? method.
	lib/PollingManager.prfm
		Fix eval-queue, if data remains in event-queue, process all remain data after
		  next event-queue process.
	lib/exec.prfm
		Fix, rewrite exit-condition-block, wait-bloc for data stream jammed.

2022/05/23
	tag:
		PRFM_20220523
		1.5.25

2022/05/24
	lib/List.prfm
		Add method List::assoc and List::assoc-value, assoc like the lisp's assoc function
		  and assoc-value is same as $foo assoc v : cdr.
	lib/exec.prfm
		Add proc-enc: named parameter for specify to process character encoding.
		Change buffer size 256 to 1024.
	pmacs update:
		Change man editor command can now specify the character encoding of
		  a sub-process by the global environment MAN_ENCODOING.
		  Default encoding is UTF-8F.
		Add config variable MAN_ENCODING at pmacs.conf and sample for FreeBSD jman settings.
		Add key map for FreeBSD kDN6, kDN7, kUP6, kUP7, kRIT6, kRIT7, kLFT6 and kLFT7.
	Makefile
		Change FreeBSD target perfumesh to static link .

2022/05/30
	pmacs update:
		Add global configuration variable FREECURSOR_ENABLE for FreeCursor mode feature
		  on/off value.
		Add if output string data including "\n" character, *Scratch* buffer is split the
		  line and output.

2022/06/02
	Makefile
		If your system have the clang compiler, use it.
		Remove libncursesw* dynamic link library file from package tar ball.
	pmacs update:
		Support Red Hat Linux man command, see pmacs.conf MAN_PREFIX variable.
	tag:
		PRFM_20220602
		1.5.26

2022/06/03
	types.c
		Change toy_clone, use cell_clone in cell.c for null data copy.
	cell.[ch]
		Add function cell_clone and cell_add_cell for cell copy with null data.
	commands.c
		Change command print/prinntln use cell_add_cell in cell.c for null data copy.
		Fix command select and accept, if receive EINTR retry system calls.
	methods.c
		Change method File::puts use fwrite(C) for null data output to file.
	lib/copy.prfm
		Add new utility, file copy can binary file copy too.
	lib/man.prfm
		Fix sub-process environment variable.

2022/06/05
	cell.[ch]
		Add function cell_cmp for cell compare with null data.
	methods.c
		Change String::{=,>,>=,<,<=,!=} compare logic become to use cell_cmp.
	pmacs update:
        	Add file decoder RAW.

2022/06/06
	methods.c, types.c
		Change GC finalizer call GC_register_finalizer_ignore_self ->
		  GC_register_finalizer_no_order, for GC warning message 
		  "GC Warning: Finalization cycle involving" suppress file and coro
		  finalize.
	commands.c
		Add pid command for return process id myself.
	lib/PipeWrapper.prfm
		Add coro closure value inject to co-routin local stack.
	lib/exec.prfm
		Add command option :err-to-stdout for error output read from sub-process,
		  if this option is specified read data is output to stdout.
	lib/sysinfo.prfm
		Add process id myself output.
	pmacs update:
		Fix grep-next, grep-previous file name in grep line process become to
		  path-adjust function for normalized.
		Chage grep, man, compile command to use pipe command for these process.
		Add pinterp.prfm output starting message sysinfo too.

2022/06/07
	Makefile
		Change make build-pkg copy shared library only.
	tag:
		PRFM_20220607
		1.5.27

2022/06/07
	Makefile
		Change detect CLANG compiler writting.
	pmacs update:
		Change pkg/INSTALL document.
		Change open-file, view-file, read-file, save-buffer, save-buffer-as,
		  grep, read-grep-file, cd and compile commands are become to
		  return the my home directory when input "~" character before "/"
		  character.

2022/06/10
	methods.c
		Change String::append! and String::. use cell_add_cell become to string concat
		  can null data included.
		Fix few syntax print.

2022/06/12
	pmacs update:
		Add ask interp kill.
		Delete create stderr chanel at grep, man, compile commands.
		Add info for info global and $PINTERP value at internal-stat.
		Add emagency exit from mini-input, press Ctrl+Shift+Alt+DEL.
		Change PINTERP value to object for delegation.
		Change PINTERP start-up code.

2022/06/13
	expand-syntax.prfm
		Add load pipe at system initialize.
	pmacs update:
		Change the argument files load at on the fly.
		Change pinterp starting message.

2022/06/14
	config.h.in
		Change signal stack size 16KB -> 64KB.
	pmacs update:
		Add pmacs terminate process.
		Add file-completion command is file completion command for file name completion at
		  *Scratch* buffer.
		Add key bind CTRL+I to file-completion at Mode-Interp.
		Add key bind Alt+i to file-completion at default mode.

2022/06/15
	methods.c
		Fix String::split make a null list if string is zero size, it's become to make
		  one element zero size string.
	pmacs update:
		Add mini-input key Ctrl+DEL is delete character at current cursor position to
		  line end.
		Fix *Scratch* buffer can't out zero size string.
	tag:
		PRFM_20220615
		1.5.28

2022/06/15
	lib/util.prfm
		Add extention files error detect.
	pmacs update:
		Change BRACKETS-PAIR color.

2022/06/16
	pmacs update:
		Change buffer-list display format.
		Change CombiWindow status bar display format.
		Add command grep-current-word for immediataly grep on cursor text,
		  and key bind to Shift+F5.
		Add command man-current-word for immediataly man on cursor text,
		  and key bind to Shift+F6.
		Change smart-move-{forward,backward} word character "-" become to including.
		Add mini-input support insert from paste buffer press key CTRL_V.

2022/06/17
	pmacs update:
		Add command select-region-word and bind to key ALT_w.
		Fix line-input routine when copy buffer is empty.

2022/06/19
	pmacs update:
		Change pdoc documentation.
		Add commands set-dos and set-nodos are set text type to DOS or UNIX.

2022/06/20
	pmacs update:
		Change remake select-region-word, become to restrict select word.
		Change delete-word, grep-current-word and man-current-word to use select-region-word.
		Add new keybinding ALT_space -> start-region for Android terminal.

2022/06/22
	pmacs update:
		Fix paste-buffer bug when if region multi line selected.
	tag:
		PRFM_20220622
		1.5.29

2022/06/24
	pmacs update:
		Change grep-current-word grep option '-rnHe' -> '-nHe' for local directory only grep.

2022/06/28
	pmacs update:
		Fix initial file order was reversed specified file list.

2022/07/05
	pmacs update:
		Change mini window foreground and background color on LIGHT theme.

2022/07/19
	pmacs update:
		Change LIGHT themes status and vertical window color.

2022/07/27
	tag:
		PRFM_20220727_PLASMA
		1.6.0

2022/08/02
	pmacs update:
		Change DARK theme color, inactive edit window foreground color.
		Change LIGHT theme color, inactive foreground color.
		Delete window number display from edit windows status line.

2022/08/14
	pmacs update:
		Delete mode BList key bind Ctrl+B and add global key bind ALT+b command is buffer-list-completion.

2022/08/16
	pmacs update:
		Add utility function apply-color for wrap curs-color and display attributes set to region.
		Change curs-color calling to apply-color call overall.
		Add utility function set-color-name for append color name to dictionary $ColorDB and append attributes.
		Change pmacs-theme.prfm use set-color-name utility function.
		Add AttrDB global variable.
		Change *::set-color method interface change, color-name value change to color-name symbol.
		Change set-theme add error handling when conf file reading.
		Change init-color-DB read user config theme file ".pmacs/theme.conf".

2022/08/17
	pmacs update:
		Change to display message delay when user config file occured error.
		Add color attributes apply at status line window.

2022/08/18
	pmacs update:
		Add regexp pattern RESERVED_KEYWORD_HILIGHT and VALUE_LITERAL_HILIGHT, add hex number pettern and
		octal number pattern.
		Add color name NORMAL_TEXT for cancel the matched resorved word hilight color.

2022/08/19
	pmacs update:
		Add new theme MODERN forked from LIGHT.

2022/08/30
	methods.c
		Change Integer and Real comparater returns always return <t> if condition is true.
	pmacs update:
		Add brackets pair hilight origin near cursor bracket too.

2022/09/06
	pmacs update:
		Add return new key symbol KEY_DEL, if key in 0x7f character pressed.
		Add delete back character, if input KEY_DEL at mini-input.

2022/09/07
	pmacs update:
		Change command key-binding-list output layout and CombiWindow.key file descriptions, 
		  change key-group item, change key-combinations item and add key-symbol item.

2022/09/08
	pmacs update:
		Change key bind,
		  Ctrl+D -> delete-character,
		  Alt+D -> key-binding-list.

2022/09/09
	pmacs update:
		Add editor command set-buffer-tab-width for set buffer's tab width localy.

2022/09/10
	tag:
		PRFM_20220910
		1.6.1

2022/09/18
	pmacs update:
		Change CombiWindow.key key group description.
		Change window status line, delete file full-path name.
		Fix free cursor mode reset at smart-newline, smart-indent and smart-dedent.
		Fix if previous line indent spec is null string, retry get indent spec from more
		  previous line at smart-newline and smart-indent.
		Change 00quick-start.pwiki document.

2022/09/19
	pmacs update:
		Add cursor point character code/EOL mark/EOF mark in window status line.

2022/09/23
	pmacs update:
		Fix key repeat acceleration algorithm.

2022/09/24
	pmacs update:
		Change keybindngs.
		  kND7 to kND4 -> smooth-scroll-up-anothor-window.
		  kND7 to kND4 -> smooth-scroll-down-anothor-window.
		Refactaring mini window input become use to mini window input utilities,
		  new file 'MiniInputUtil.prfm' these functions are including:
		    mini-input-until-enter, mini-input-y-or-n and mini-input-yes-or-no.

2022/09/25
	pmacs update:
		Separate EditorCommandDefs.prfm file and rename to pmacs-command.prfm
		  and pmacs-command-{buffer,dired,document,edit,file,misc,move,search,util,window}.prfm files.
		Add error handling at pmacs-command.prfm file load.
		Fix BufferBase::set-raise no set timestamp if already set 0(low) specified.
		Change file completion buffer name *Directory* to *Completion* and set raise low.
		Change trace editor command output file name to "pmacs-trace.txt".

2022/09/30
	pmacs update:
		Add editor command descriptions.
		Add key-binding-list display format and display editor command description.

2022/10/04
	auto-encoding.prfm
		Add new utility function for auto detect file encoding.
	pmacs update:
		Add new utility MiniInputUtil::mini-input-file-chooser is file selecter for
		  editor command.
		Change to use mini-input-file-chooser these commands:
		  open-file, view-file, read-file, save-buffer, save-buffer-as,
		  read-grep-file, grep, cd and compile.
		Add file encoding auto detect by new utility auto-encoding.
		Add editor command set-buffer-encoding for specify encoding buffer internal.
		Add guard, if file is not regular when file read.
2022/10/05
	pmacs update:
		Fix auto encode detect at read-file command.

2022/10/06
	tag:
		PRFM_20221006
		1.6.2

2022/10/09
	pmacs update:
		Fix file stat error if file not exists when open-file and read-file command.
		Add *Completion* buffer cleaner at mini-input-file-chooser.

2022/10/13
	lib/mkdir.prfm,rmdir.prfm
		Add file operation utilitys:
		  mkdir.prfm is create specified the directory recursively.
		  rmdir.prfm is remove specified the directory recursively.
	pmacs update:
		Add dired-rename-file and dired-delete-file command.
		Add dired-mark-file, dired-delete-marked-files and dired-update-window command.
		Add file mark API in DiredBuffer and identify the marked file in window.
		Add/Delete key bind on Dired-mode:
		  "d" -> dired-delete-file
		  "r" -> dired-rename-file
		  " " -> dired-mark-file (delete next-page and previous-page bindings)
		  "X" -> dired-delete-marked-files
		  "u" -> dired-update-window
		Change Dired header format.
		Add color DB name DIRED_MARKED.

2022/10/14
	pmacs update:
		Add dired clear mark if dired-rname-file or dired-delete-file executed.
		Add dired "." and ".." files are not to mark file.
		Change dired item and header format.
		Add mini-input-fully-yes-or-no keyin-proc.
		Add dired-clear-all-marks command.
		Add dired-delete-marked-files can use up-allow and down-allow for view marked files.
		Fix file-completion if before file name character is '\t' can't completion.
		Fix if file is symbolic link then travarse link.

2022/10/16
	pmacs update:
		Change DiredBuffer::get-marked-files becomes return sorted list.
		Change dired-delete-marked-files failed delete files becomes to display separate comma.

2022/10/17
	pmacs update:
		Change mini-input-file-chooser and dired-delete-marked-files key bind:
		  KEY_UP -> smooth-scroll-up
		  KEY_DOWN -> smooth-scroll-down
		  kDN5, KEY_NPAGE -> move-half-up
		  kUP5, KEY_PPAGE -> move-half-down
		Fix EditBuffer::load-content use file path becams to real file path for travarse sym-link.
		Add get-real-path utility to WUtil for travarse sym-link.

2022/10/19
	pmacs update:
		Fix some sym-link file open.

2022/10/20
	pmacs update
		Add dired-open-other-window editor command.
		Add binding: "o" -> dired-open-other-window on Dired mode.
		Add Window::get-window-list, Window::get-window-list-without-current.

2022/10/23
	commands.c
		Add file command option 'rstat' for file status return real node information.
	pmacs update:
		Change key repeat parameter.
		Change EditBuffer::load-content buffer naming.

2022/10/24
	pmacs update:
		Add editor commands are dired-change-mode, dired-delete-dir and dired-make-dir.
		Add key bindings:
		  'm' on dired mode -> dired-change-mode
		  'D' on dired mode -> dired-delete-dir
		  'M' on dired mode -> dired-make-dir
		Change status line format, dismis '(', ')' and column number to left.
		Add new utility function get-path-condition is return path condition, real-path and message,
		  that judge follow symbolic link and file type by specified path.
		Change dired-open, dired-open-other-window, open-file, view-file and read-file use to
		  get-path-condition.
		Fix smart-newline and smart-indent if previous line has white space, do more previous line detect.  

2022/10/25
	pmacs update:
		Fix dired-delete-dir prompt spell miss.
		Add macOS support from [[@https://github.com/shugo]].
	tag:
		PRFM_20221025
		1.6.3

2022/10/27
	*
		Add support another co-routine library 'coru'. If use to coru, try define "CORU_USE = yes" in Makefile.
		  --> See: [[@https://github.com/geky/coru]]
	commands.c
		Add support __APPLE__ for file stat|rstat command.

2022/10/28
	extlib/coru
		Add co-routine stack free flag in coru struct.
		Add __APPLE__ compile switch in coru_platform.c.
	types.c
		Change coding style.

2022/11/02
	pmacs update:
		Add parameterized vertical bar separate character config, config name is VERT_BAR_CHAR default "|".

2022/11/12
	*
		Add new data type INTR is interrupt occurred meening.
	commands.c
		Add intr? command for condition judge INTR data type when interrupt occured.
	cstack.c
		Add aarch64 support, aarch64 need C stack alignment 16 bytes.
		Add cstac_get_slot_size for return stack size per slot.
	eval.c
		Add return INTR data type when occured itimer interval interruput.
	lib/slist.prfm
		Add display item Avail is available stack memory size.
	extlib/coru
		Add aarch64 platform.

2022/11/17
	lib/Object.prfm
		Add Object::instance? class-name method for judge myself object inherit specified class-name.
	pmacs update:
		Change dired file order split directory and other type.

2022/11/24
	lib/Stream.prfm
		Add Stream::clear-queue for clear queue data.
		Add Stream::ready-write? for write access ready?
		Add Stream::gets-nowait for return queue data as soon.
	lib/expand-syntax.prfm
		Add load-tasklet command.
		Fix load-class, load-func command.
	pmacs update:
		Add SysCall gets, puts.
		Add Task::set-sleep.
		Add TaskManager::force-wait-task for waiting task migrate to sleep state check.

2022/11/27
	Makefile
		Add install lib/Tasklet/*.
	lib/Stream.prfm
		Fix Stream::clear for set eof indicator.
		Add Stream::clear-queue for clear queue.
		Fix Stream::ready? for return condition queue pooling only.
		Fix Stream::puts,gets race condition code.
		Add Stream::gets-nowait if no data return <nil> as soon.
		Add Stream::stat comode property.
	pmacs update:
		Add column length limit to EditStream, set property method is set-columnlimit.
		Add EditStream::puts core logic to atomic.
		Add P(Pause) state to Task. And control meshods are Task::set-pause / set-resume,
		  control commands are suspend.prfm / resume.prfm.
		Change ts output format.

2022/11/28
	pmacs update:
		Add returnable system-call result at Task.prfm/SysCall.prfm/TaskManager.prfm.
		Add new system-call new-task.

2022/11/29
	pmacs update:
		Add new-task syscall can specify argument to sub task.
		Add system call wait for wait child task exit.

2022/11/30
	pmacs update:
		Add SysCall return value at sleep, apply-editor, new-task, wait.
		Add sysCall throw errors at apply-editor, new-task, task script need catch
		  exception if use these system calls.
		Update pmacs.conf INTERP_ITIMER_INTERVAL 20 -> 10.
		Update pmacs.conf BLINK_FACT 4 -> 5.

2022/12/01
	pmacs update:
		Add user startup script file load.

2022/12/03
	global.[ch], interp.c
		Add new global variable BASE_PATH for indicate the pmacs interpriter install base directory.
	lib/Stream.prfm
		Fix Stream::clear for re-initialize queue count, queue element and eof mark.
		Add Stream::set-nocomode to unset co-mode flag.
	pmacs update:
		Fix TaskManager delete $SysReq clear-queue call, aggregate to $SysReq clear by Stream.prfm fix.
		Change initialize DEFAULT_PWIKI_PATH from $BASE_LIB variable.

2022/12/05
	lib/Stream.prfm
		Delete method Stream::clear-queue, to use Stream::clear.
	pmacs update:
		Change Tasklet/move-message, if left char width greater equal 2 then delete it and add " ".
		Fix Window::key-event-loop refactaring and brackets-pair occurd some events supress.
		Change pmacs.conf INTERP_ITIMER_INTERVAL, Linux -> 10, FreeBSD -> 50.

2022/12/06
	pmacs update:
		Add SysCall wait-any for wait any child tasks. If waitable tasks are all done return <nil>.
		Add task and non task environment current working directory save/restore.

2022/12/07
	commands.c
		Add disable-itimer command for disable interval timer signal no sencitive.
	pmacs update:
		Change PipeWrapper.prfm for co-routine set property to enable-itimer.

2022/12/08
	lib/pipe.prfm, PipeWrapper.prfm
		Fix if input and output channel jammbed in pipe command, 
		  do change pause -> pause (SLEEP 0).
	pmacs update:
		Add "who" column to ts.prfm command, It's display tasks waiting target.

2022/12/09
	eval.c
		Change if eval body is null statement (e.g. {}) execute "false;" statement.
	lib/Stream.prfm
		Fix Stream::puts if has shortcut var then use it, if no has shortcut
		  and has :nonewline option set itemcount initial '1'.
	pmacs update:
		Change CombiWindow::display-evaluation call co-routine interpriter execute atomicaly.

2022/12/12
	pmacs update:
		Change branckets-pair call to background from Window.
		Add dummy method WindowBase::get-window-size.
		Change buffer-list mode column size.
		Add mode 'Console' it's buffer name is '*Console*'.

2022/12/13
	methods.c
		Add File::set-tag and File::get-tag for object identify from application.
	lib/Stream.prfm
		Add Stream::set-tag and Stream::get-tag for object identify from application.

2022/12/15
	pmacs update:
		Add dired-copy-link command and bind to 'p' key in dired mode.

2022/12/16
	lib/Stream.prfm
		Add Stream::clear-eof only eof flag to clear.
		Fix Stream::close to itemcount reset.
	pmacs update:
		Add SysCall::gets if file has tag name STDIN hook for wait std-input.
		Add Task::delivery-stdin for input stream delivery to task's stdin.
		Add TaskManager::force-wait-task delivery stdin data to task if task is wait from stdin data.
		Add TaskManager::send-to-task, recv-from-stdin, stdin-ready? for delivery stdin data.

2022/12/21
	lib/String.prfm
		Add method bool, list, dict and vector for string constracted data structure convert
		  to native data.
	pmacs update:
		Add pmacs config database management.
		  WUtil.prfm: add setconf and unsetconf function for config database operation.
		  pmacs-command-util.prfm: add editor command list-config and set-config.
		    list-config is show all configuration settings.
		    set-config is change the configuration setting.
		  pmacs.conf: use setconf utility and discard use defvar command.
		Add error handle in pmacs.conf when read the user config pmacs.conf.

2022/12/23
	tag:
		PRFM_20221223_K34
		1.7.0

2022/12/26
	pmacs update:
		Add EditStream::get-buffer method.
		Add Task Since instance set initialize now time.
		Change Task::release to Task::terminate and exec terminate proc in terminate method.
		Add Task::{set-stdin,set-stdout,set-stdrrr} for Task driver utility API.
		Add Task::set-terminate-proc for Task driver utility API.
		Change Task::set-stdio if set to <nil> in in, out, error parameter, do not set instance variable.
		Change editor command grep implimentation use Task system.

2022/12/27
	pmacs update:
		Change TaskManager after dispatch process order, SysCall request -> task exit.
		Change editor command grep, grep-current-word and compile implimentation use Task system.
		  and create new function do-command-async.
		Add new config variable COMPILE_COMMAND for compile command throw shell string.

2022/12/28
	pmacs update:
		Fix Task::set-terminate and terminate method bug.
		Change TaskManager apply-syscalls error handle output Task_stdout if task exited.
		Change TaskManager apply-syscalls new-task migration stdio channel parent to child.
		Add new config variable COMPILE_COMMAND_DIR for compile command execute directory.
		Change compile editor command use setconf COMPILE_COMMAND, COMPILE_COMMAND_DIR.
		Fix do-command-async function if window not same buffer binding current window.

2022/12/30
	pmacs update:
		Change TaskManager output message format.

2023/01/02
	pmacs update:
		Change MainWindow bracket-pair call do not delayed process.
		Change INPUT_TIMEOUT 20 -> 30.

2023/01/04
	pmacs update:
		Change CombiWindow.key aplphabetic, alphabetic+alt, alphabetic+shift+alt order of key descriptor.
                Add editor command delete-word-after-current-point.

2023/01/12
	pmacs update:
		Change divide Window::key-event-loop to keyin-proc and keyin-apply methods.
		Change KEY_REPEAT_ACCEL value set use setconf.

2023/01/13
	pmacs update:
		Add SysCall accept is accept socket fd and return File object in task.
		Add SysCall recv is recieve socket stream from File object in task.
		Change TaskManager, child name repair number start 1.
		Add lib/Tasklet/pmacs-server.prfm
		Add lib/pmacs-client.prfm
		Add lib/pmacs-server-config.prfm

2023/01/14
	Makefile
		Add pmacs and pmacs-client install to $(PREFIX)/bin directory.
	pmacs update:
		Add BufferBase::set-unregist-proc, BufferBase::get-unregist-proc are do auto execute procedure API
		  at buffer killed timing.
		Change BufferManager::unregist-buffer call BufferBase's unregist procedure if buffer unregisted.
		Add lib/Tasklet/pmacs-server.prfm for remote file open function under pmacs.
		Add pkg/pmacs.in and lib/pmacs-client.prfm are for remote file open control external command.
		Add lib/pmacs-server-config.prfm for pmacs-server configuration file.
		Add exec :bacground option is no wait child process option for exec utility.
		Change editor commands git-pull, git-push and git-commit entering previous directory and use
		  pmacs-server function can git comment input in pmacs.
		Change pmacs.conf for git-* command configuration.

2023/01/16
	Makefile
		Change 'make install' install shell name rename to pmacs-install.sh next removed.
	commands.c
		Add :noblock option to connect command for non-block INET conectin, if use this
		  option next use connect? command.
		Add connect? command for connect :noblock after tell INET connection status.
	pmacs update:
		Add SysCall connect for non-block INET connection make.
		Add socket buffer length to lib/pmacs-client.prfm.
		Add SysCall gets support 'SOCK' tag has File stream, it's same as SysCall recv.
		Change pmacs-server use SysCall recv -> SysCall gets.
		Add git-diff and git-status editor command.

2023/01/19
	pmacs update:
		Add config variable ALWAYS_SMART_INDENT. If it's <t>, always indent current line when
		  carriage return inserted.
		Change smart-indent use ALWAYS_SMART_INDENT.
		Fix Tasklet/console-cleaner.prfm buffer clean execute in SysCall::apply-editor.
		Add smart-insert omit if terminal paste.

2023/01/21
	pmacs update:
		Change unsetconf no unset CONFIG_DESC.
		Fix ALWAYS_SMART_INDENT do auto indent but if cursor position at last and before 
		  character was " " or "\t" then insert tab-space.
		Change theme MODERN and MODERN-DARK, ACTIVE_VERT_LINE_PROTECT.
		Change config valiable MAX_WINDOW_DIVS to read-only.

2023/01/24
	pmacs update:
		Fix backup search string when cancel search mode.
		Fix smart-indent current indent level wrong if previous line is label ('[A-Za-z0-9_]+:').

2023/01/29
	pmacs update:
		Change key bind file name CombiWindow.key -> pmacs-default.key and COMBIWINDOW_KEYBINDFILE
		  variable name to DEFAULT_KEYBINDFILE and add USER_KEYBINDFILE variable.
		Change search-next/search-previous key binding to Ctrl+F5/Ctrl+F6.
		Change key-binding-list sort order. Fn key bind to grouped and DEL,INS,PgUp,PgDn,HOME,END,allow
		  key groups to less order.
2023/02/02
	pmacs update:
		Change EditStream if puts args >= 2, convert string sirealize it's args.
		Change bindings F7 -> search-next, F8 -> search-previous.
		Fix mini-input-callback no such variable 'result'.
		Add install $(HOME)/.pmacs template file install script.
		Add $(HOME)/.pmacs/* files install template.

2023/02/15
	tag:
		PRFM_20230215_A34
		1.7.1

2023/02/15
	pmacs update:
		Add repeat-command and set-times editor command, for execute previous editor
		  command specified repeat times (default 1 time). Key bindings are Alt+x to repeat
		  command and ESC to specify times.

2023/02/17
	pmacs update:
		Add exchange-region editor command for exchange start region point and current point
		  key bind to ALT+x and repeat-command re-assign to ALT+.

2023/02/20
	pmacs update:
		Add CombiWindow.prfm and do-repeat (in pmacs-command-util.prfm) to keyboard 
		  macro recoding caller.
		Add Window.prfm to keyboard macro recording callee.
		Add editor command start-recording (binding to ALT+1), end-recording (binding to ALT+0)
		  and execute-record (binding to ALT+comma).
		Add key binding ALT+1 (start-recording), ALT+0 (end-recording) and ALT+comma (execute-record).
		Add config variable IGNORE_RECORD for not recording commands define.

2023/02/23
	pmacs update:
		Add new feature key prefix binding.

2023/02/24
	pmacs update:
		Move CombiWindow::setkeydesc to new file lib/pmacs-keydesc.prfm.
		Add new file lib/pmacs-keydesc.prfm separate from lib/pmacs-default.key.
		Add CombiWindow::{get-prefix-list,get-bind-map,get-keybyind,get-key-desc}.
		Add load-keydesc call from WUtil::load-keydesc.
		Change key-binding-list support for key prefix list.
		Change show-keycode but support default key prefix only yet.
		Fix key-binding-list layout.
		Add config variable BINDING_MODE, if set emacs choose emacs binding mode other pmacs mode.

2023/02/27
	lib/String.prfm
		Add format indicator to String::expand string, syntax is ${var[:ind]}.
		  e.g. --- "${var:%10d}" expand 
	pmacs update:
		Add display P(macs) or E(macs) indicator in window status line.
		Add start-recording-for-append and assign key bind to ALT+2.

2023/02/28
	pmacs update:
		Change status line and buffer-list modify mark (*) and protect mark (%) position change.
		Add command set-prefix-desc for if input prefix key display description string in mini-window.
		Add cancel-prefix and pop-prefix for key in prefix control editor command.
		Add if CTRL+G input in mini-window do exit mini-input routine.
		Define key bind emacs mode each prefix + CTRL_G for cancel-prefix.
		Add prefix menu on pmacs mode.

2023/03/01
	pmacs update:
		Add CombiWindow::get-key-bindings for create and return command assigned key bindings.
		Change key-binding-list display [[&command]] link for key bindings show.
		Change pwiki-click-link if [[?command]] linked this, show command assigned key bindings
		  to mini-window buffer.
		Add command menu implimentated by prefix key.

2023/03/02
	pmacs update:
		Add config variable PMACS_MENU for show menu at ESC- prefix key pressed.
		Add replace-buffer-with-query editor command for replace buffer by user query.
		Add MiniInputUtil::mini-input-until-cancel, and refactaring replace-buffer-with-query
		  by use mini-input-until-cancel.

2023/03/03
	pmacs update:
		Add search-next and search-previous becomes to hilibht to found text.

2023/03/04
	pmacs update:
		Fix get-next-search-length if SEARCH_MODE is text add option :nocase.
		Add new tasklet star.prfm.

2023/03/05
	pmacs update:
		Delete dupulicate window render/refresh.
		Change TaskManager dispatch all runnable tasks.

2023/03/08
	pmacs update:
		Change config values:
		  INPUT_TIMEOUT(30->10),
		  INTERP_ITIMER_INTERVAL(30->20), 
		  BLINK_FACT(4->5).
		Change ncurses::curs_keyin const value no_input_count 3 -> 8.
		Change pmacs-server wait child for SysCall wait.
		Add new tasklet 'cd-test' for task independent current directory.

2023/03/13
	pmacs update:
		Fix key-binding-list long key name format leaked.
		Add pmacs menu item [o]ther -> [!]shell.

2023/03/14
	pmacs update:
		Change key-binding-list 2 lines display all commands section.

2023/03/15
	pmacs update:
		Change separate function get-keybind-string use from pwiki-click-link.
		Change key-binding-list use get-keybind-string function and list key bindings
		  list directly when list all commands section.

2023/03/16
	pmacs update:
		Add theme color item 'INVERT' for EDIT_BUFFER inverted color.
		Change search-{next,previous} spot color from SEARCH_CURRENT_HILIGHT to INVERT.
		Fix execute-record local variable isolate.

2023/03/17
	interp.c
		Fix move GC_malloc -> GC_MALLOC and delete GC_free.

2023/03/21
	tag:
		PRFM_20230321
		1.7.2

2023/03/22
	pmacs update:
		Change key-binding-list format, all commands section display key name.
		Change emacs keybind mode KEY_ESC g and ALT+g separated.
		Add utility function getkeydesc at pmacs-keydesc.

2023/03/31
	pmacs update:
		Add system call 'SysCall update-screen' for update editor screen.
		Add force update screen when output data to '*Console*'.
		Add SysCall update-screen to pmacs-server, console-adjuster and star tasklets.
		Change screen update timing when no key input alived exec background or exec tasks.
		Add pinterp watch-dog return result NONE.
		Add background result ALL.

2023/04/02
	pmacs update:
		Add mouse event driver.

2023/04/03
	pmacs update:
		Add mouse operation features.
		  Add mouse handling feature to Window.prfm.
		  Add new mouse command - 
		    mouse-smooth-scroll-up
		    mouse-smooth-scroll-down
		    mouse-point
		  Add new keybindngs to pmacs-default.key and pmacs-keydesc.prfm files.
		  Add new config variable for mouse operation MOUSE_WHEEL_COUNT.

2023/04/04
	pmacs update:
		Add main window last mouse state memory and access function set-last-mouse-event
		  and get-last-mouse-event.
		Change non operation editor command name nop -> .nop
		Change key-binding-list not show start "." editor command.
		Add editor commands mouse-point-region, mouse-point-region-append and mouse-cut-or-paste.
		Add new key bind:
		  mouse-point-region <- Mouse1-Rel
		  copy-region <- Mouse3-Rel	
		  mouse-point-region-append <- Ctrl+Mouse1-Prs
		  mouse-cut-or-paste <- Ctrl+Mouse3-Rel
		Add mouse browsing command mouse-pwiki-click-link and mouse-man-click-link.
		Add mouse browsing key bind in Mode-PWiki.key, Mode-Interp.key and Mode-Man.key.

2023/04/05
	pmacs update:
		Add hit count to search command prompt.
		Add buffer-list buffer name clickable.
		Add link back button bind to man mode, pwiki mode, dired mode.
		Fix brackets-pair command (array boundary error).

2023/04/07
	pmacs update:
		Fix if search result string is null, omit it.

2023/04/12
	pmacs update:
		Add dired clickable file name.
		Add mouse-dired-click-link command.
		Add $HOME/.perfume install at pkg install.sh script.

2023/04/14
	lib/Object.prfm
		Fix cond ?? nil-condition-part if cond is block and after evaluation
		  is symbol then return after evaluation symbol.
		Fix cond ??? then-condition-part else-condition-part if cond is block 
		  and after evaluation is symbol then return then-condition-part.
	pmacs update:
		Change if no buffer mode evaluation yet display mode to "-".

2023/04/17
	pmacs update:
		Add key bindings ALT_c -> list-config, ALT_s -> set-config.

2023/04/18
	pmacs update:
		Change list-config format.

2023/04/24
	pmacs update:
		Change config value BUFFER_LINK and DIRED_FILE_LINK to read only.
		Change list-config format.

2023/05/01
	lib/NullStream.prfm
		Add new Stream class for no effect I/O.
	lib/exec.prfm
		Change option paramater err-to-stdout: Bool -> err-to: Stream for output error
		  to programable.
	lib/pipe.exec
		Change accectable in: and out: parameter for Stream instance (include NullStream).
	pmacs update:
		Fix paste-buffer if region selected and (sy sx) > (ey ex) occurd wrong paste string.
		Change man editor command for man output error to stderr to ignore.
		Change OS type detect logic use pipe out: parameter given NullStream object.
	tag:
		PRFM_20230501
		1.8.0

2023/05/11
	pmacs update:
		Change task dispatch algorythm, one task dispatch at one scheduled iteration.
		Change task dispatch algorythm, reset itimer before task dispatch.

2023/05/12
	pmacs update:
		Change Task::string format.
		Fix restore previous State at Task::resume if before Task::suspend.
		Fix LastDispatch time unit sec -> msec.
		Change curs-keyin BLINK_FACT unit, bit count -> msec.

2023/05/15
	commands.c
		Add neq? command for if not equal two parameters return <t>.
	pmacs update:
		Change mode key-map Mod-Interp.key file. Dismis Perfume keyword hilight.

2023/05/16
	pmacs update:
		Change buffer-list header and body line format indicator commonized.

2023/05/17
	lib/hanoi.prfm, lib/hanoiui.prfm
		Add new game.
	lib/String.prfm
		Add String::repeat from pmacs WUtil.
	commands.c
		Fix leak file descripter which fork-exec sub process, add FD_CLOEXEC to file descriptor at connect, socket and accept functions.
	pmacs update:
		Change buffer-list display help text use get-keybind-string for buffer-list-completion key-bind and buffer-list-delete key-bind.
		Remove String::repeat from WUtil.

2023/05/18
	Makefile
		Fix for valgrind debug run that clang14 output valgrind unreadable debug info by DWARF5, change DWARF4 CFLAG is -gdwarf-4.

2023/05/19
	*
		Fix struct initialize after GC_MALLOC for valgrind memory checker less messages.

2023/05/20
	pmacs update:
		Change vertical line separate character and vertical line colors.

2023/05/23
	ncurses.c
		Change repeat detect times, 8 -> 4.
	pmacs update:
		Change pmacs-server message, (git-commit) -> (pmacs-server).
		Add EditStream::set-prompt/get-prompt for process prompt holder if input line use :nonewline set.
		Change man command use task, omit use pipe.
		Add terminal command for unix shell command execute in buffer.
		Add new Mode 'Term' for terminal editor command.

2023/05/24
	lib/exec.prfm
		Change wait timer value 50 -> 5.
	pmacs update:
		Add terminal-enter-command set mark before command execute.
		Add add-prefix command for Mode file prefix key define.
		Add Mode-Term.key prefix Ctrl+\ for send key event.
		Change man async procedure do not update screen untile man command end.
		Add editor commands are:
		  terminal-send-break: send break key code to terminal process -> Ctrl+\ Ctrl+C
		  terminal-send-eof: send eof key code to terminal process -> Ctrl+\ Ctrl+D
		  terminal-send-quit: send quit key code to terminal process -> Ctrl+\ Ctrl+\
		  terminal-send-suspend: send suspend key code to terminal process -> Ctrl+\ Ctrl+Z
		Change pmacs.conf terminal shell values.
		Change no menu mode prefix define.
		Add cd command simuration at terminal-enter-command.

2023/05/25
	pmacs update:
		Change MODERN and MODERN-DARK themes inactive window border color.

2023/05/26
	pmacs update:
		Add OS type indicate config value name is OSTYPE. eg) "Linux", "FreeBSD"...
		Fix ColLimit over process at EditStream::puts.
		Fix shell-terminal exec omit err-to: arg.
		Fix prompt detect if input length less than prompt length.

2023/05/29
	exec.prfm
		Add pid-proc: {body} for internal process id given to block body.
	methods.c
		Change File::gets for property if noblock is <t> case, no block return.
	pmacs update:
		Add Task::{set,get}-client for task client data setter/getter.
		Fix sub shell pid wait when shell exited.

2023/05/30
	pmacs update:
		Change Window::resize-window to atomicaly.
		Change terminal task EditStream::puts override method to SysCall apply-editor block.
		Fix sub shell kill before pid wait at terminal editor command.

2023/05/31
	exec.prfm
		Add option exec verbose: {| message | log-output-body}.
	pmacs update:
		Add Task::output-log method.
		Add verbose output given for exec command terminated.

2023/06/02
	commands.c
		Add environ: ("name=val" ...) option for set sub-process environment variable to fork-exec command.
		Add kill and killpg command for send signal to process or process group.
	lib/consts.prfm
		Add signal value for send signal command kill and killpg.
	lib/exec.prfm
		Add environ: ("name=value" ...) option for set sub-process environment variable to exec utility.
	pmacs update:
		Change exec option given spec for exec utility, use environ: option.
		Add TERMINAL_ENV config for terminal shell given environment variable.
		Fix send terminal code support for FreeBSD, Ctrl+C and Ctrl+\, but not support code Ctrl+Z.
		Change terminal shell kill impl use kill $SIGHUP command.

2023/06/03
	pmacs update:
		Fix terminal puts to buffer from sub-shell read line impl.

2023/06/05
	tag:
		PRFM_20230605
		1.8.1

2023/06/06
	interp.c
		Fix argv map to $ARGV make decode encoding type with DEFAULT_DIRENT_ENCODING.

2023/06/08
	perfumesh.c
		Add args encoding by decode_dirent util function, default encoding is UTF-8.

2023/06/11
	pmacs update:
		Change status line format, ommit key-bind mode and add "Mode:" to mode indicator.

2023/06/12
	pmacs update:
		Change status line 'Mode' format, add $KEYBIND_MODE prefix.

2023/06/14
	pmacs update:
		Change pmacs-server/pmacs-client if file is directory occurd an error '5'.

2023/06/20
	lib/expand-syntax.prfm
		Delete :notrace option from load-tasklet.
	methods.c, types.[ch]
		Add Coro::get-stacktrace for get the stack trace info from co-routine if occurd it.
	pmacs update:
		Add get-stacktrace for get the co-routin occurd exception stacktrace.
		Add log output Task stack trace if occurd it in Task.

2023/06/23
	pmacs update:
		Fix *Scratch* buffer line limit separate bug when output is multi lines.

2023/06/29
	pmacs update:
		Fix pmacs-client if argument is relative path then make full path.

2023/07/05
	pmacs update:
		Add previous buffer name to help buffer.
		Add previous buffer name to key-binding-list buffer.
		Add make-command-pdoc for create template editor command PWiki file.

2023/07/18
	pmacs update:
		Change TERMINAL_ENV environment value TERM=pmacs -> TERM=dumb.
		Add keybind ALT_v to show-keycode.
		Add start message to terminal editor command.

2023/07/23
	pmacs update:
		Add keybind Ctrl+Alt+[↑] -> top-of-buffer, Ctrl+Alt+[↓] -> bottom-of-buffer.

2023/08/28
	pmacs update:
		Fix EditBuffer::get-content-dir if result is directory then add "/" to last.
		  But if directory is "/" do not it.
		Fix ScratchBuffer::get-content-dir if result is not "/" then add "/" to last.
		Fix line-input utility function, if intput key is CTRL_G then convert to KEY_ESC send.
		Fix get-current-buffer-dir utility function, add return path "/" to last, if directory
		  is not "/".
		Change bellow editor commands to use EditBuffer::get-content-dir method for 
		  mini-input-file-chooser initial value.
		  Commands are: open-file, view-file, save-buffer, save-buffer-as
		Change read-grep-file command if initial directory is "/" do not append "/" to last.
		Change grep command if initial directory is "/" do not append "/" to last.
		Change cd command if initial directory is "/" do not append "/" to last.

2023/08/30
	pmacs update:
		Fix EditBuffer::get-currentdir and get-current-buffer-dir calling, do not repare last "/".

2023/09/06
	commands.c
		Add wait command to new option :noblock for non-block mode child process wait,
		  and return value format change int::exitstatus => list::(pid exitstatus).
	lib/exec.prfm, pipe.prfm
		Change call wait command.
	lib/telnet.prfm, telnet-server.prfm
		Add sample socket application for telnet file delivery server and client.

2023/09/18
	pmacs update:
		Add editor commands, all-window-scroll-up, all-window-scroll-down, all-window-scroll-right,
		  all-window-scroll-left, all-window-page-down, all-window-page-up, all-window-top-of-buffer,
		  all-window-bottom-of-buffer. Key bind to Ctrl+Shift+Alt+[↑],[↓],[→],[←],[PgDn],[PgUp],[HOME],[END].

2023/09/19
	methods.c
		Change unknown character font width (u+fffd width to 2).
	pmacs update:
		Fix execute-macro if window forcus change, set new window for apply.
		Change unknown character font and combining character font.

2023/09/20
	fclib.[ch]
		New feature font glyph width calibration and get library.
	Makefile
		Add compile fclib.[ch] and install misc/default.fcab file.
	pmacs update:
		Change font width get method XOPEN library wcwidth(3) change to use font calibration
		  library function fcl_get_width(3).

2023/10/04
	error.h
		Add error define TE_BADENCODELESSLENGTH if decode UTF-8/EUC-JP/Shift-JIS occurd error,
		  it meaning less byte after character return this.
	encode.[ch]
		Add encoding name EUC-JPF and Shift-JISF for fake return if occured decode error.
	methods.c
		Add String::udecode for decode RAW encoding string to specified encode string.
		Add String::uencode for Unicode string to specified encode and return RAW encoding string.
	pmacs update:
		Change encoder EUC-JP -> EUC-JPF, Shift-JIS -> Shift-JISF.

2023/10/05
	encode.[ch]
		Change encoding define, UTF-8F -> UTF-8/F, EUC-JPF -> EUC-JP/F, Shift-JISF -> Shift-JIS/F.
	methods.c
		Add implement RAW I/O stream input mode in File::gets.
	pmacs update:
		Change encoding using name UTF-8F -> UTF-8/F, EUC-JPF -> EUC-JP/F, Shift-JISF -> Shift-JIS/F.

2023/10/15
	pmacs update:
		Change set-times editor command to display guidance.

2023/10/16
	pmacs update:
		Add key-record at execute-editor-command if command executed.

2023/10/19
	lib/copy.prfm
		Change file mode set to RAW I/O mode for input.

2023/11/02
	pmacs update:
		Change editor command name:
		  move-half-up -> half-next-page
		  move-half-dwon -> half-previous-page
		  move-half-right -> half-scroll-right
		  move-half-left -> half-scroll-left

2023/11/08
	pmacs update:
		Add editor commands are: search-word-forward and search-word-backward,
		  search text or regexp string specified value at mini buffer.
		Change editor commands are: search-forward and search-backward,
		  if buffer length large than $SEARCH_WORD_THRESHOLD, change search methods to
		  search-word-forward or search-word-backward.
		Change mini input key binding, Ctrl+[DEL] to clear mini buffer all text.
		Add key bindings are:
		  Alt+f -> search-word-forward
		  Alt+r -> search-word-backward
		Add config value SEARCH_WORD_THRESHOLD.

2023/11/19
	pmacs update:
		Add editor command buffer-list-save for bind to buffer and call save-buffer.
		Add key bind at BList mode "s" to buffer-list-save.

2023/11/21
	pmacs update:
		Add Window::set-mini-input-end-hook method and execute $MiniInputEndHook
		  at mini input end timming afger 100 msec background.
		Change buffer-list-save to use function save-buffer next call buffer-list
		  use set-mini-input-end-hook.
		Change save-buffer editor command to use saparated function save-buffer.
		Add new function save-buffer separated from editor command save-buffer.

2023/11/24
	pmacs update:
		Change search-word-* UI, input previous search word if press CTRL_F or CTRL_R.

2023/11/27
	pmacs update:
		Add new window split pattern, command name and bindings are:
		  split-tridivide1-window <- ALT_F1
		  split-tridivide2-window <- ALT_F2
		  split-tridivide3-window <- ALT_F3
		  split-tridivide4-window <- ALT_F4

2023/11/29
	pmacs update:
		Fix execute-record, delay next step macro execute at after execute mini-input editor command.

2023/11/30
	pmacs update:
		Fix execute-record in mini-input mode.
		Add editor command terminal-bottom-of-buffer <- Ctrl+Alt+[↓], Ctrl+[END].

2023/12/13
	Makefile
		Chagen package files permission.

2023/12/14
	pmacs update:
		Fix get-dir function if path name not equal "/" add "/" to last it.

2023/12/23
	tag:
		PRFM_20231223_K35
		1.9.0

2024/01/02
	pmacs update:
		Add C style multi line comment syntax hilight.
		Add 'Mode-C.key' and 'Mode-Java multi line comment hilight regexp.

2024/01/05
	pmacs update:
		Add :debug option to show-message function, for output log to *Console* buffer.

2024/01/17
	doc/manual.{odt,pdf}
		Change text font mincho(NOTO font) to gothic(yu gothic font).
	pmacs update:
		Change BLACKETS_PAIR color at theme MODERN and MODERN-DARK.

2024/01/18
	pmacs update:
		Add pwiki link [[@URL]] for execute $WEB_BROWSER_EXEC config value with URL.

2024/01/21
	commands.c
		Add control-type? command for get control type indicator.

2024/01/29
	ncurses.c
		Change curs-clear, curs-wipe and curs-bgclolor few optimized, become to flicker less at
		  refresh timing.

2024/01/31
	pmacs update:
		Change config variable WEB_BROWSER_EXEC(string) -> WEB_BROWSER_LIST(list of string).

2024/02/14
	tag:
		PRFM_20240214
		1.9.1

2024/02/20
	pmacs update:
		Change man document, grep buffer early display at first page.
		Add toplevel prefix key description string ("").
		Change mouse operation link click from MOUSE_Prs_1 to MOUSE_Rel_1
		  at BList mode, Dired mode, Interp mode, Man mode, PWiki mode and default mode.

2024/02/21
	ncurses.c
		Change unknown character and combinated character substitute glyph, and define glyph width macro.

2024/02/28
	pmacs update:
		Add show-version editor command.
		Add show error message if pmacs-server occured at server socket create.

2024/03/01
	*
		Delete HAS_GCACHE option.
	hash.[ch]
		Change hash's internal pointer array becomes to has value array, for cache hit ratio.

2024/03/06
	pmacs update:
		Change BUFFER_LINK regex and buffer-list header string.

2024/03/22
	eval.c
		Change message at ErrStackOverflow error.
	pmacs update:
		Change BRACKETS_PAIR color at theme MODERN, MODERN-DARK and LIGHT.
		Change key repeat accel coding style.
		Add \t key input cancel if SMART_INDENT_CANCEL is true at file-completion command.

2024/04/03
	pmacs update:
		Add set mark command in top-of-buffer, bottom-of-buffer and select-region-all.
		Fix BUFFER_LINK regex.
		Add buffer name fixer, delete space first and bottom in buffer name,
		  replace '<', '>' to '(', ')' in buffer name.
		Add CTRL_D key bind for delete character into mini-buffer input.

2024/04/09
	encoding.[ch]
		Add encoding UTF-16LE, UTF-16LE/F, UTF-16BE and UTF-16BE/F.
	methods.c
		Add encoding UTF-16LE, UTF-16LE/F, UTF-16BE and UTF-16BE/F at File::gets and File::puts.
	pmacs update:
		Add encoding UTF-16LE/F and UTF-16BE/F.

2024/04/19
	methods.c
		Add File:seek method for seek file position.

2024/04/20
	methods.c
		Add File::getb and File::putb, for get or put byte stream from/to file.
	lib/auto-encoding.prfm
		Add detect BOM for encoding type UTF-16LE and UTF-16BE.

2024/06/10
	pmacs update:
		Fix man-current-word window layout bug.

2024/06/18
	pmacs update:
		Add all-command-list editor command, divide from key-binding-list command.
		Add terminal-cursor-down, terminal-cursor-right for terminal mode key bindings.

2024/06/26
	doc/manual.*
		Update.
	pmacs update:
		Change *Scratch* buffer start message.
	tag:
		PRFM_20240626
		1.10.0

2024/06/28
	pmacs update:
		Rename command dired-open-other-window -> dired-open-next-window.
		Add command dired-open-previous-window.
		Add keybind O at dired mode for open file previous window.
		Add command store-buffer for store buffer name at current window.
		Add command bind-stored-buffer for bind stored buffer at current window.
		Add keybind ALT+m to store-buffer.
		Add keybind ALT+M to bind-stored-buffer.
		Add config variable BUFFER_MEMORY for buffer name store.
		Change buffer-list header format.
		Add store-buffer and bind-stored-buffer command for memory buffer name and
		  bind the memrey buffer name to windodw.
		Add buffer-list-store-buffer for memory buffer name in *Buffer List* buffer.
		Add buffer-list-open-next-window and buffer-list-open-previous-window for
		  open another window in *Buffer List* buffer.
		Add keybind "o" to buffer-list-open-next-window at BList mode.
		Add keybind "O" to buffer-list-open-previous-window at BList mode.

2024/06/29
	pmacs update:
		Add keybind Alt+(mouse scroll down) -> all-window-scroll-down.
		Add keybind Alt+(mouse scroll up) -> all-window-scroll-up.
		Add keybind Ctrl+Alt+(mouse scroll down) -> all-window-page-down.
		Add keybind Ctrl+Alt+(mouse scroll up) -> all-window-page-up.

2024/06/30
	pmacs update:
		Add mouse-buffer-click-next-window and mouse-buffer-click-previous-window.
		Add keybind Ctrl+(mouse 1 release) -> mouse-buffer-click-next-window.
		Add keybind Alt+(mouse 1 release) -> mouse-buffer-click-previous-window.
		Change DiredBuffer format, like a unix 'ls -l'.

2024/07/02
	pmacs update:
		Add mouse-dired-click-next-window and mouse-dired-click-previous-window.
		Add keybind Ctrl+(mouse 1 release) -> mouse-dired-click-next-window.
		Add keybind Alt+(mouse 1 release) -> mouse-dired-click-previous-window.

2024/07/06
	array.[ch]
		Add method array_insert and array_delete.
	methods.c
		Add method Vector::insert! and Vector::delete! for insert or delete at specified item.
	pmacs update:
		Change Vector::insert-string-at, Vector::delete-character-at and Vector::delete-region
		  becomes to use Vector::insert! and Vector::delete! methods.

2024/07/13
	pmacs update:
		Fix undo/redo buffer operation bugs.

2024/07/14
	tag:
		PRFM_20240714
		1.11.0

2024/07/21
	pmacs update:
		Change status line format.

2024/08/04
	pmacs update:
		Change string leteral regexp at Interp/C/Java/Prfm mode.

2024/08/05
	parser.c
		Add RQUOTE parse escape character '\t', '\n' and '\r'.
	pmacs update:
		Fix exchange-region.

2024/08/15
	lib/hexdump.prfm
		Fix perfomance.
	pmacs update:
		Change mouse event handling in editor command code.
		Change bracket-pair execute on the time.
	tag:
		PRFM_20240815
		1.11.1

2024/09/24
	pmacs update:
		Add display task start message to *Console* buffer.

2024/10/30
	pmacs update:
		Add keybind [F10] -> buffer-list.

2025/09/01
	lib/String.prfm
		Add syntax String::replace
			String replace [:all] 'pattern' {| e | replace-body}
		The matched pattern is replaced by the result of processing by replace-body, 
		e is the part of matched string.
	pmacs update:
		Add editor command decode-uquoted-encoding is replace some \uXXXX style strings
		to UTF-8 string.

2025/12/06
	doc/intro.txt
		Add introduction text.
	commands.c
		new command accept object argument. The method deliver to specified class or object.
		  syntax: new [class-name | object]
	types.[ch], global.[ch]
		Add 'Self' instance variable to new any object.
		Add integer_to_str_base utility function.
	list.c
		Add list_get method.
	methods.c
		Add Integer::string-base for convert integer to string with specified N-base.
		Add String::int-base for convert string to integer with specified N-base.
		Fix List::get if has dot pair.
		Add String::get like Stiring::at but difficult return <nil> if out of index.
	parser.c
		Fix toy_parse_getmacro becomes to nest get macros. (ex) $o,1,2 -> [[$o get 1] get 2]
	lib/Integer.prfm, Real.prfm, Bool.prfm, util.prfm
		Add new file and add new methods Integer::len, Integer::get, Real::len, Real::get.
		Add new file and add new methods Bool::len, Bool::get.
	lib/Mode-C.key, Mode-Java.key
		Fix sigle quote literal hilight regexp.
	lib/Mode-Text.key
		Add pwiki link hilight and pwiki-click-link command.
	pmacs update:
		Change input-character-code command, if input TAB key then input TAB code to buffer.
		Change goto command key binding CTRL_G -> Alt_g

2025/12/14
	parser.c
		Change coding style.Change coding style.
	types.c
		Change BIND tag to_print, to_string, to no terminate ";" BIND object.
	pmacs update:
		Add untabify editor command.
		Fix EditBuffer::get-buffer-tab-width if no set TABs return DEFAULT_TAB_WIDTH.
		Fix mose-point editor command, no reset region mode.

2025/12/23
	cell.[ch]
		Add cell_truncate function.
	methods.c
		Add commnad String::truncate for truncate N size characters from string.
	parser.c, types.c
		Change rquote syntax change, the \ character is no longer an escape character unless it is preceded
		by an apostrophe character.
	pmacs update:
		Update Mode files, Mode-C.key, Mode-Interp.key, Mode-Java.key, Mode-Prfm.key, Mode-Text.key and pmacs.conf
		  for rquote specification change.
		Update truncate editor command.
	tag:
		PRFM_20251223_K37
		1.11.2

#eof
